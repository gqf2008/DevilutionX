# M20: Automap System 移植计划

## 📊 当前状态

**已完成**:
- M14: Dialogue System
- M15: Missiles Enhancement
- M16: Game Loop
- M17: Lighting System
- M18: Rendering Integration
- M19: Save/Load System

**当前进度**: ~64,149 行 / 125,500 行 ≈ **51%**

---

## 🎯 M20 目标: Automap System

### C++ 源文件分析

| 文件 | 行数 | 职责 |
|------|------|------|
| `automap.cpp` | 1,956 | 自动地图渲染和交互 |
| `automap.h` | ~100 | 头文件 |

### 核心功能

1. **AutomapTile** - 地图瓦片类型定义 (60+ 种类型)
2. **MapColors** - 地图颜色常量
3. **Drawing Functions** - 绘制函数 (菱形、门、桥、河流等)
4. **AutomapView** - 已探索区域追踪
5. **DrawAutomap()** - 主渲染函数
6. **Player Arrow** - 玩家方向指示器

---

## 📐 架构设计

### 数据结构

```rust
/// 自动地图瓦片类型
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(u8)]
pub enum AutomapTileType {
    None = 0,
    Diamond,
    Vertical,
    Horizontal,
    Cross,
    FenceVertical,
    FenceHorizontal,
    Corner,
    // ... 60+ 种类型
    CaveHorizontalCross,
    CaveVerticalCross,
    Bridge,
    River,
    Lava,
    PentagramClosed,
    PentagramOpen,
}

/// 瓦片标志
bitflags! {
    pub struct AutomapTileFlags: u8 {
        const VERTICAL_DOOR = 1 << 0;
        const HORIZONTAL_DOOR = 1 << 1;
        const VERTICAL_ARCH = 1 << 2;
        const HORIZONTAL_ARCH = 1 << 3;
        const VERTICAL_GRATE = 1 << 4;
        const HORIZONTAL_GRATE = 1 << 5;
        const DIRT = 1 << 6;
        const STAIRS = 1 << 7;
    }
}

/// 地图颜色
pub struct MapColors {
    pub player: u8,
    pub bright: u8,
    pub dim: u8,
    pub item: u8,
    pub pentagram_open: u8,
    pub lava: u8,
    pub water: u8,
    pub acid: u8,
}

/// 自动地图管理器
pub struct AutomapManager {
    pub active: bool,
    pub map_type: AutomapType,
    pub view: [[u8; DMAXY]; DMAXX],
    pub scale: i32,
    pub minimap_scale: i32,
    pub offset: Displacement,
    pub minimap_rect: Rectangle,
    tile_types: [AutomapTile; 256],
}
```

---

## 📝 实现计划

### Phase 1: 数据结构 (300 lines, 8 tests)

- [ ] `AutomapTileType` enum (60+ variants)
- [ ] `AutomapTileFlags` bitflags
- [ ] `MapColors` 常量
- [ ] `AutomapTile` struct

### Phase 2: 绘制函数 (500 lines, 10 tests)

- [ ] `draw_diamond()` - 菱形
- [ ] `draw_vertical_door()` - 垂直门
- [ ] `draw_horizontal_door()` - 水平门
- [ ] `draw_dirt()` - 泥土像素
- [ ] `draw_bridge()` - 桥
- [ ] `draw_river_*()` - 河流系列
- [ ] `draw_lava_*()` - 岩浆系列

### Phase 3: 管理器 (400 lines, 8 tests)

- [ ] `AutomapManager::new()`
- [ ] `init_automap_once()` - 初始化
- [ ] `init_automap()` - 加载瓦片数据
- [ ] `start_automap()` / `stop_automap()`
- [ ] `update_automap_view()` - 更新可见区域

### Phase 4: 渲染 (600 lines, 10 tests)

- [ ] `draw_automap()` - 主渲染
- [ ] `draw_automap_tile()` - 单瓦片渲染
- [ ] `draw_automap_player()` - 玩家箭头
- [ ] `draw_automap_text()` - 文字信息
- [ ] `draw_minimap()` - 小地图

### Phase 5: 交互 (200 lines, 4 tests)

- [ ] `automap_up/down/left/right()` - 移动
- [ ] `automap_zoom_in/out()` - 缩放
- [ ] `set_automap_view()` - 设置可见

---

## 🔗 依赖关系

```
M20 Automap
    ├── M17 Lighting (DungeonLevelType)
    ├── levels/gendung (dungeon data)
    ├── player (position, direction)
    └── engine/render (drawing primitives)
```

---

## ✅ 预期输出

| 指标 | 目标 |
|------|------|
| 新增代码 | ~2,000 lines |
| 测试数 | ~40 tests |
| 文件数 | 1 file (automap.rs) |
| C++ 覆盖 | automap.cpp (1,956 lines) |

---

## 📅 时间估计

| Phase | 估计时间 |
|-------|----------|
| Phase 1 | 20 min |
| Phase 2 | 40 min |
| Phase 3 | 30 min |
| Phase 4 | 45 min |
| Phase 5 | 15 min |
| **总计** | ~2.5 hours |

---

*计划创建: 2025-12-04*
