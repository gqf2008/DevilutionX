# M46: 滚动对话文本系统移植 - 完成报告

## 1. 概述

**M46目标**: ✅ 精确移植 Diablo 的滚动对话文本系统 (minitext)。

**完成状态**: ✅ 已完成

**源文件分析**:

- `minitext.cpp` (~170行) - 滚动对话文本实现

**Rust文件**:

- `src/game/minitext.rs` (~629行，含15个测试)

## 2. 技术分析

### 2.1 全局状态

```cpp
bool qtextflag;              // 是否正在显示滚动文本
int qtextSpd;               // 滚动速度 (ms/px)
uint32_t ScrollStart;       // 滚动开始时间
OptionalOwnedClxSpriteList pTextBoxCels;  // 文本框图形
std::vector<std::string> TextLines;       // 文本行
```

### 2.2 常量

```cpp
const int LineHeight = 38;  // 行高（像素）
```

### 2.3 核心函数

| C++ 函数 | Rust 函数 | 说明 |
|----------|-----------|------|
| `LoadText(text)` | `load_text(text)` | 加载并换行文本 |
| `CalculateTextSpeed(sfx)` | `calculate_text_speed(sfx)` | 计算滚动速度 |
| `CalculateTextPosition()` | `calculate_text_position()` | 计算当前位置 |
| `DrawQTextContent(out)` | `draw_qtext_content(out)` | 绘制文本内容 |
| `FreeQuestText()` | `free_quest_text()` | 释放资源 |
| `InitQuestText()` | `init_quest_text()` | 初始化 |
| `InitQTextMsg(m)` | `init_qtext_msg(speech_id)` | 开始显示消息 |
| `DrawQTextBack(out)` | `draw_qtext_back(out)` | 绘制背景 |
| `DrawQText(out)` | `draw_qtext(out)` | 主绘制函数 |

### 2.4 依赖分析

- `Surface` - 渲染表面（已有）
- `Point/Displacement` - 坐标类型（已有）
- `_speech_id` - 语音ID枚举（需要）
- `SfxID` - 音效ID（需要简化）
- `ClxSprite` - 精灵渲染（模拟）

## 3. Rust 实现计划

### 3.1 文件结构

```
src/game/
├── mod.rs           # 添加 minitext 模块
└── minitext.rs      # 滚动文本系统 (~250行)
```

### 3.2 Rust 结构

```rust
/// 行高（像素）
pub const LINE_HEIGHT: i32 = 38;

/// 滚动文本状态
pub struct QuestTextState {
    /// 是否正在显示
    pub active: bool,
    /// 滚动速度 (ms/px)
    pub speed: u32,
    /// 开始时间
    pub start_time: u32,
    /// 文本行
    pub lines: Vec<String>,
}

impl Default for QuestTextState {
    fn default() -> Self;
}

impl QuestTextState {
    pub fn new() -> Self;
    pub fn load_text(&mut self, text: &str, max_width: i32);
    pub fn start(&mut self, speed: u32, current_time: u32);
    pub fn stop(&mut self);
    pub fn calculate_position(&self, current_time: u32) -> i32;
    pub fn is_finished(&self, current_time: u32) -> bool;
}
```

## 4. 测试计划

| 测试名称 | 验证内容 |
|----------|----------|
| `test_initial_state` | 初始状态正确 |
| `test_load_text_single_line` | 单行文本加载 |
| `test_load_text_wrap` | 文本换行 |
| `test_calculate_position` | 位置计算 |
| `test_is_finished` | 完成检测 |
| `test_start_stop` | 启动/停止 |

## 5. 预计工作量

- minitext.rs: ~250行
- 测试: ~100行
- 预计完成时间: 30分钟

## 6. 执行状态

- [x] 创建 minitext.rs
- [x] 添加到 mod.rs
- [x] 编译验证 (无错误)
- [x] 测试 (15个测试)
- [x] 更新文档

## 7. 代码统计

| 文件 | 行数 | 测试数 |
|------|------|--------|
| minitext.rs | ~629 | 15 |
