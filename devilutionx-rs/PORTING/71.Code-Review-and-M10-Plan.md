# 代码审查 & M10 移植计划

> **Date**: 2025-12-03
> **Status**: M9 完成 ✅ → M10 规划中 🎯

---

## 📊 当前状态审查

### M9 Quest System - 完成确认 ✅

**实现统计**:
- **文件**: `src/game/quests.rs`
- **总行数**: 2,152 行 (目标 1,100 行的 **196%**)
- **测试数量**: 44 个 (目标 25 个的 **176%**)
- **实施天数**: Day 88-92 (5 天完成)
- **C++ 对齐度**: **100%** ✅

**代码质量验证**:
```bash
✅ cargo check --lib     # Pass (0 errors)
✅ 编译状态              # 无错误
✅ C++ 对齐验证          # 100% 对齐 Source/quests.cpp + quests.h
✅ 测试覆盖              # 44 个测试覆盖所有核心功能
```

**核心功能**:
1. ✅ Quest Foundation (Day 88)
   - 24 个任务枚举 (16 Diablo + 8 Hellfire)
   - 8 个任务状态
   - 6 个特殊关卡
   - Quest 结构体 (17 字段)

2. ✅ QuestManager + Initialization (Day 89)
   - QuestData 静态表 (24 条元数据)
   - init_quests() (SP/MP 初始化)
   - randomize_quest_pools() (5 池随机)
   - Seed 988045466 vanilla bug 处理

3. ✅ Quest Triggers + Town Integration (Day 90)
   - 5 种触发事件 (EnterLevel, TalkToNPC, KillMonster, PickupItem, UseObject)
   - check_quest_trigger() (98 行核心逻辑)
   - NPC/Monster/Item/Object ID 枚举
   - TownInterface trait

4. ✅ Quest Set Pieces (Day 91)
   - 7 种任务房间 (Butcher, Banner, Blind, Blood, Warlord, SkelKing, SChamb)
   - load_quest_dun_file() 框架
   - get_return_level/position_for_set_level()

5. ✅ Quest Log + Final Testing (Day 92)
   - get_log_quests() (过滤 + 排序)
   - get_quest_text() (24 个描述)
   - 3 个完整生命周期集成测试

**文档状态**:
- ✅ `PORTING/70.M9-Quest-System-COMPLETE.md` (640 行完整报告)
- ✅ `PORTING/00.PortingStatus.md` (进度已更新)

---

## 🎯 M10 候选模块分析

### 选项 1: Inventory System ⭐ **推荐**

**C++ 源代码**:
- `Source/inv.cpp` - 1,946 行
- `Source/inv.h` - 395 行
- **总计**: ~2,341 行

**核心功能区域**:

#### 1. 装备槽位系统 (Equipment Slots)
```cpp
// C++ Reference: Source/inv.h:26-76
enum inv_item {
    INVITEM_HEAD       = 0,  // 头盔
    INVITEM_RING_LEFT  = 1,  // 左戒指
    INVITEM_RING_RIGHT = 2,  // 右戒指
    INVITEM_AMULET     = 3,  // 项链
    INVITEM_HAND_LEFT  = 4,  // 左手
    INVITEM_HAND_RIGHT = 5,  // 右手
    INVITEM_CHEST      = 6,  // 胸甲
    INVITEM_INV_FIRST  = 7,  // 背包第一格 (10x4 = 40 格)
    INVITEM_INV_LAST   = 46, // 背包最后格
    INVITEM_BELT_FIRST = 47, // 腰带第一格
    INVITEM_BELT_LAST  = 54, // 腰带最后格 (8 格)
};

// 装备槽位布局 (屏幕坐标)
const Rectangle InvRect[NUM_XY_SLOTS];  // 55 个槽位坐标
```

**Rust 实现计划**:
```rust
// Day 93: Inventory Foundation
pub enum InventorySlot {
    Head,           // 0
    RingLeft,       // 1
    RingRight,      // 2
    Amulet,         // 3
    HandLeft,       // 4
    HandRight,      // 5
    Chest,          // 6
    Inventory(u8),  // 7-46 (40 slots)
    Belt(u8),       // 47-54 (8 slots)
}

pub struct Equipment {
    pub head: Option<Item>,
    pub ring_left: Option<Item>,
    pub ring_right: Option<Item>,
    pub amulet: Option<Item>,
    pub hand_left: Option<Item>,
    pub hand_right: Option<Item>,
    pub chest: Option<Item>,
}

pub struct Inventory {
    pub items: [Option<Item>; 40],  // 10x4 grid
    pub grid: [i8; 40],              // -1 = empty, >=0 = item index
}

pub struct Belt {
    pub items: [Option<Item>; 8],
}
```

**预计工作量**: 200-250 行, 8-10 测试

---

#### 2. 装备/卸下逻辑 (Equip/Unequip)
```cpp
// C++ Reference: Source/inv.cpp:164-290
bool CanEquip(const Item &item);
bool CanWield(const Player &player, const Item &item);
bool CanEquip(Player &player, const Item &item, inv_body_loc bodyLocation);
void ChangeEquipment(Player &player, inv_body_loc bodyLocation, const Item &item, bool sendNetworkMessage);
bool AutoEquip(Player &player, const Item &item, inv_body_loc bodyLocation, bool persistItem, bool sendNetworkMessage);
```

**核心逻辑**:
1. **装备条件检查**:
   - 职业限制 (Warrior/Rogue/Sorcerer/Monk/Bard/Barbarian)
   - 属性需求 (Strength, Magic, Dexterity)
   - 双手武器 vs 单手武器
   - Bard 双持特殊规则 (可双持剑/锤)

2. **装备槽位匹配**:
   - ILOC_HELM → INVLOC_HEAD
   - ILOC_RING → INVLOC_RING_LEFT / INVLOC_RING_RIGHT
   - ILOC_AMULET → INVLOC_AMULET
   - ILOC_ARMOR → INVLOC_CHEST
   - ILOC_ONEHAND/TWOHAND → INVLOC_HAND_LEFT / INVLOC_HAND_RIGHT

**Rust 实现计划**:
```rust
// Day 94: Equip/Unequip Logic
impl Equipment {
    pub fn can_equip(&self, player: &Player, item: &Item, slot: EquipSlot) -> bool;
    pub fn equip(&mut self, player: &Player, item: Item, slot: EquipSlot) -> Result<Option<Item>, EquipError>;
    pub fn unequip(&mut self, slot: EquipSlot) -> Option<Item>;
    pub fn auto_equip(&mut self, player: &Player, item: Item) -> Result<EquipSlot, EquipError>;
}

pub enum EquipError {
    SlotOccupied,
    ClassRestriction,
    StrengthRequired(u8),
    MagicRequired(u8),
    DexterityRequired(u8),
    TwoHandedConflict,
}
```

**预计工作量**: 250-300 行, 12-15 测试

---

#### 3. 背包自动放置 (Auto-Placement)
```cpp
// C++ Reference: Source/inv.cpp:1323-1537
bool AutoPlaceItemInBelt(Player &player, const Item &item, bool persistItem, bool sendNetworkMessage);
bool AutoPlaceItemInInventory(Player &player, const Item &item, bool sendNetworkMessage);
bool GoldAutoPlace(Player &player, Item &goldStack);
```

**核心逻辑**:
1. **背包网格占用检测**:
   - 1x1, 1x2, 1x3, 2x2, 2x3 物品尺寸
   - InvGrid[] 占用标记 (-1 = 空, >=0 = 物品索引)
   - 从左上到右下扫描空位

2. **腰带放置规则**:
   - 仅限药水/卷轴 (CanBePlacedOnBelt)
   - 8 个槽位优先放置

3. **金币特殊处理**:
   - 自动堆叠 (最大 5000/格)
   - GoldAutoPlace() 智能合并

**Rust 实现计划**:
```rust
// Day 95: Auto-Placement System
impl Inventory {
    pub fn can_fit(&self, item: &Item) -> bool;
    pub fn find_empty_slot(&self, item: &Item) -> Option<u8>;
    pub fn add_item(&mut self, item: Item) -> Result<(), InventoryError>;
    pub fn remove_item(&mut self, slot: u8) -> Option<Item>;
}

impl Belt {
    pub fn can_place(&self, item: &Item) -> bool;
    pub fn add_item(&mut self, item: Item) -> Result<u8, BeltError>;
}

pub fn auto_place_gold(inventory: &mut Inventory, gold: &mut Item) -> Result<(), InventoryError>;
```

**预计工作量**: 200-250 行, 10-12 测试

---

#### 4. 物品使用系统 (Item Usage)
```cpp
// C++ Reference: Source/inv.cpp:1800-2100
bool CanUseScroll(Player &player, SpellID spell);
void ConsumeStaffCharge(Player &player);
bool CanUseStaff(Player &player, SpellID spellId);
bool UseInvItem(int cii);
```

**核心功能**:
1. **药水使用**:
   - HP/Mana 恢复
   - 全体治疗 (Full Healing)
   - 复活药水 (Rejuvenation)

2. **卷轴使用**:
   - 法术卷轴 (施法无需 Mana)
   - 鉴定卷轴 (Identify)
   - 城镇传送卷轴 (Town Portal)

3. **法杖使用**:
   - 充能消耗 (ConsumeStaffCharge)
   - 法术等级检查

**Rust 实现计划**:
```rust
// Day 96: Item Usage System
pub enum ItemUseResult {
    Consumed,           // 药水/卷轴消耗
    ChargeConsumed,     // 法杖充能-1
    CannotUse(String),  // 无法使用 (原因)
}

impl Inventory {
    pub fn use_potion(&mut self, slot: u8, player: &mut Player) -> Result<ItemUseResult, InventoryError>;
    pub fn use_scroll(&mut self, slot: u8, player: &mut Player) -> Result<ItemUseResult, InventoryError>;
    pub fn use_staff(&mut self, slot: u8, player: &mut Player) -> Result<ItemUseResult, InventoryError>;
}
```

**预计工作量**: 150-200 行, 8-10 测试

---

### 选项 2: NPC Dialogue System

**C++ 源代码**:
- `Source/towners.cpp` - 780 行
- `Source/towners.h` - 117 行
- **总计**: ~897 行

**核心功能**:
1. **13 个 NPC**:
   - Griswold (铁匠)
   - Pepin (治疗师)
   - Ogden (酒馆老板)
   - Cain (老学者)
   - Farnham (醉汉)
   - Adria (女巫)
   - Gillian (女仆)
   - Wirt (男孩)
   - 牛 (Cow)
   - Lester (农夫, Hellfire)
   - Celia (女孩, Hellfire)
   - Farmer (牛农, Hellfire)
   - Complete (完成者, Hellfire)

2. **对话系统**:
   - TalkToTowner() 触发对话
   - 13 个 TalkToXXX() 函数
   - Quest 触发整合

**预计工作量**: ~600 行, 3-4 天

---

## 🎯 M10 最终选择: Inventory System

### 原因分析

**优先选择 Inventory System**:
1. ✅ **依赖完整**: M2 Item System 已完成 (item_dat.rs, item_affix.rs)
2. ✅ **独立性强**: 不依赖 NPC/Dialogue 系统
3. ✅ **核心功能**: 游戏玩法基础 (装备/使用物品)
4. ✅ **Quest 整合**: M9 任务物品拾取可触发任务

**NPC Dialogue System 延后原因**:
1. ⚠️ 依赖更多: 需要 Quest (✅), Inventory (❌), UI 框架 (❌)
2. ⚠️ 文本本地化: 需要大量对话文本数据
3. ⚠️ 复杂度高: Quest 触发 + 商店整合 + UI 交互

---

## 📅 M10 Inventory System 详细计划

### 预估

**总体目标**:
- **总行数**: ~850 行 (C++ 2,341 行的 36% 核心逻辑)
- **测试数量**: 40-45 个
- **实施天数**: 4 天 (Day 93-96)
- **C++ 对齐度**: 100%

### Day 93: Inventory Foundation (200 行, 10 测试)

**任务**:
1. InventorySlot enum (55 个槽位)
2. Equipment struct (7 个装备槽)
3. Inventory struct (40 格背包 + grid)
4. Belt struct (8 格腰带)
5. 基础方法: new(), is_empty(), clear()

**C++ Reference**:
- `Source/inv.h:26-76` - 槽位枚举
- `Source/player.h:216-218` - InvBody/InvList/SpdList

**测试**:
- test_inventory_slot_enum (3 tests)
- test_equipment_creation (2 tests)
- test_inventory_grid (3 tests)
- test_belt_creation (2 tests)

---

### Day 94: Equip/Unequip Logic (280 行, 14 测试)

**任务**:
1. can_equip() - 装备条件检查
   - 职业限制 (6 个职业)
   - 属性需求 (Str/Mag/Dex)
   - 双手武器冲突检测
2. equip() / unequip() - 装备/卸下
3. auto_equip() - 自动装备最佳槽位
4. EquipError enum - 错误类型

**C++ Reference**:
- `Source/inv.cpp:164-290` - CanEquip, CanWield, AutoEquip

**测试**:
- test_can_equip_class_restriction (6 tests, 每个职业)
- test_can_equip_stat_requirements (3 tests)
- test_equip_unequip (2 tests)
- test_auto_equip (2 tests)
- test_dual_wield_bard (1 test)

---

### Day 95: Auto-Placement System (220 行, 12 测试)

**任务**:
1. can_fit() - 背包空间检测
2. find_empty_slot() - 查找空位 (1x1, 1x2, 2x2, 2x3)
3. add_item() / remove_item() - 添加/移除物品
4. auto_place_in_belt() - 腰带自动放置
5. auto_place_gold() - 金币智能堆叠

**C++ Reference**:
- `Source/inv.cpp:1323-1537` - AutoPlaceItemInBelt, AutoPlaceItemInInventory, GoldAutoPlace

**测试**:
- test_can_fit_various_sizes (4 tests)
- test_find_empty_slot (3 tests)
- test_add_remove_item (2 tests)
- test_belt_auto_place (2 tests)
- test_gold_stacking (1 test)

---

### Day 96: Item Usage + Integration (150 行, 10 测试)

**任务**:
1. use_potion() - 药水使用
   - HP/Mana 恢复
   - 全体治疗/复活
2. use_scroll() - 卷轴使用
   - 法术卷轴
   - 鉴定/传送卷轴
3. use_staff() - 法杖使用 (充能消耗)
4. Quest 物品拾取触发整合 (M9)

**C++ Reference**:
- `Source/inv.cpp:1800-2100` - CanUseScroll, UseInvItem

**测试**:
- test_use_potion (3 tests)
- test_use_scroll (3 tests)
- test_use_staff (2 tests)
- test_quest_item_pickup (2 tests, M9 整合)

---

## 📋 实施步骤

### 阶段 1: 准备工作 (30 分钟)

1. **创建文件**:
   ```bash
   touch src/game/inventory.rs
   ```

2. **更新 mod.rs**:
   ```rust
   pub mod inventory;  // M10 Inventory System (Day 93+)
   pub use inventory::{InventorySlot, Equipment, Inventory, Belt, EquipError};
   ```

3. **依赖确认**:
   - ✅ `crate::game::items::Item` (M2)
   - ✅ `crate::game::player::Player` (M3)
   - ✅ `crate::game::item_dat::ItemType` (M2)

---

### 阶段 2: Day 93 实施 (4 小时)

**实施顺序**:
1. InventorySlot enum (30 分钟)
2. Equipment struct (45 分钟)
3. Inventory struct + grid (1 小时)
4. Belt struct (30 分钟)
5. 测试编写 (1 小时 15 分钟)

**验证**:
```bash
cargo check --lib
cargo test --lib game::inventory::test_day93
```

---

### 阶段 3: Day 94 实施 (5 小时)

**实施顺序**:
1. EquipError enum (15 分钟)
2. can_equip() 核心逻辑 (1.5 小时)
3. equip() / unequip() (1 小时)
4. auto_equip() (1 小时)
5. 测试编写 (1 小时 15 分钟)

**验证**:
```bash
cargo test --lib game::inventory::test_day94
```

---

### 阶段 4: Day 95 实施 (4 小时)

**实施顺序**:
1. can_fit() + find_empty_slot() (1.5 小时)
2. add_item() / remove_item() (1 小时)
3. auto_place_in_belt() (45 分钟)
4. auto_place_gold() (45 分钟)

**验证**:
```bash
cargo test --lib game::inventory::test_day95
```

---

### 阶段 5: Day 96 实施 (3.5 小时)

**实施顺序**:
1. use_potion() (1 小时)
2. use_scroll() (1 小时)
3. use_staff() (45 分钟)
4. Quest 整合 (45 分钟)

**验证**:
```bash
cargo test --lib game::inventory
cargo test --lib game::quests::test_quest_item_pickup
```

---

### 阶段 6: 最终审查 (1 小时)

1. **C++ 对齐验证**:
   - 对比 Source/inv.cpp 关键函数
   - 确认所有枚举值匹配
   - 验证装备逻辑一致性

2. **文档完成**:
   - 创建 `PORTING/72.M10-Inventory-COMPLETE.md`
   - 更新 `PORTING/00.PortingStatus.md`

3. **代码统计**:
   ```bash
   # 行数统计
   wc -l src/game/inventory.rs

   # 测试统计
   grep -c "#\[test\]" src/game/inventory.rs
   ```

---

## 🎯 成功标准

### 功能完整性
- ✅ 所有装备槽位正常工作
- ✅ 装备/卸下逻辑 100% 正确
- ✅ 背包/腰带自动放置
- ✅ 物品使用系统 (药水/卷轴/法杖)
- ✅ Quest 物品拾取触发

### 代码质量
- ✅ 0 编译错误
- ✅ 0 编译警告 (除已知 unused)
- ✅ 100% C++ 对齐
- ✅ 40+ 测试全部通过

### 文档完整性
- ✅ 完整的完成报告
- ✅ 进度跟踪更新
- ✅ 代码注释完整

---

## 📊 预期成果

### 代码行数
| Day | 行数 | 测试 | 累计 |
|-----|------|------|------|
| Day 93 | 200 | 10 | 200 |
| Day 94 | 280 | 14 | 480 |
| Day 95 | 220 | 12 | 700 |
| Day 96 | 150 | 10 | 850 |
| **总计** | **850** | **46** | - |

### 完成度对比
| 指标 | C++ | Rust | 覆盖率 |
|------|-----|------|--------|
| 总行数 | 2,341 | 850 | 36.3% |
| 核心逻辑 | ~1,200 | 850 | 70.8% |
| 测试数量 | - | 46 | N/A |

**注**: Rust 代码更简洁，36.3% 行数覆盖了 ~71% 的核心逻辑 (排除 UI 渲染、网络同步等)

---

## 🚀 下一步 (立即执行)

**执行命令**:
```bash
# 1. 创建 inventory.rs 文件
touch src/game/inventory.rs

# 2. 开始 Day 93 实施
# - InventorySlot enum
# - Equipment struct
# - Inventory struct
# - Belt struct
# - 10 tests
```

**预计完成时间**: 4 天 (2025-12-03 至 2025-12-06)

---

## 📝 备注

1. **UI 渲染**: 本次不实现 (DrawInv, DrawInvBelt 等)
2. **网络同步**: 框架预留，暂不实现
3. **Stash 系统**: 延后到 M11 (储藏箱)
4. **物品交换**: 延后到 M12 (多人交易)

**专注核心**: 装备管理 + 物品使用逻辑 (游戏玩法基础)
