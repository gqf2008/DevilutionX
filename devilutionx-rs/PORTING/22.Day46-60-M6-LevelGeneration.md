# Day 46-60: M6 Level Generation System (关卡生成系统)

**目标**: 实现完整的地牢生成系统，能够生成单层可玩地牢

**预计时间**: 15 天 (Day 46-60)
**预计代码**: ~8,000 lines
**预计测试**: ~100 tests

---

## 📊 C++ 源码分析

### Source/levels/ 模块统计 (11,414 lines)

| 文件 | 行数 | 功能 | 优先级 |
|------|------|------|--------|
| **gendung.cpp** | 763 | 地牢生成核心 | ⭐⭐⭐ 最高 |
| **drlg_l1.cpp** | 1,220 | Cathedral (教堂) 生成 | ⭐⭐⭐ 最高 |
| **drlg_l2.cpp** | 2,760 | Catacombs (地下墓穴) 生成 | ⭐⭐ 高 |
| **drlg_l3.cpp** | 2,095 | Caves (洞穴) 生成 | ⭐⭐ 高 |
| **drlg_l4.cpp** | 1,152 | Hell (地狱) 生成 | ⭐⭐ 高 |
| **town.cpp** | 354 | Tristram (城镇) 生成 | ⭐⭐ 高 |
| **themes.cpp** | 890 | 主题房间系统 | ⭐ 中 |
| **crypt.cpp** | 794 | Crypt (墓穴特殊区域) | ⭐ 中 |
| **setmaps.cpp** | 156 | Quest 特殊地图 | ⚠️ 低 |
| **trigs.cpp** | 856 | 传送门/楼梯触发器 | ⭐ 中 |
| **tile_properties.cpp** | 75 | 瓦片属性 | ⭐⭐ 高 |
| **reencode_dun_cels.cpp** | 299 | 瓦片重编码 | ⚠️ 工具 |

### 核心数据结构 (需从 gendung.h 移植)

```cpp
// 地牢瓦片类型
constexpr int MAXTILES = 2048;
constexpr int MAXDUNX = 112; // 地牢宽度
constexpr int MAXDUNY = 112; // 地牢高度

// 主数据结构
extern BYTE dungeon[MAXDUNX][MAXDUNY];     // 地牢瓦片层
extern BYTE dFlags[MAXDUNX][MAXDUNY];       // 地牢标记
extern BYTE dPlayer[MAXDUNX][MAXDUNY];      // 玩家位置
extern BYTE dMonster[MAXDUNX][MAXDUNY];     // 怪物位置
extern BYTE dObject[MAXDUNX][MAXDUNY];      // 物品位置
extern BYTE dItem[MAXDUNX][MAXDUNY];        // 物品位置
extern BYTE dLight[MAXDUNX][MAXDUNY];       // 光照
extern BYTE dPreLight[MAXDUNX][MAXDUNY];    // 预光照
extern int8_t dTransVal[MAXDUNX][MAXDUNY];  // 透明度值

// 墙和地板
extern int16_t dPiece[MAXDUNX][MAXDUNY];    // 瓦片索引
extern MICROS dblock[MAXDUNX * 2][MAXDUNY * 2]; // 微块数据

// 房间/走廊数据
extern int drlgRoomNum;
extern DrlgRoom drlgRooms[DRLG_MAX_ROOMS];
```

---

## 🎯 Day 46-60 分解计划

### 📅 **Phase 1: Core Foundation (Day 46-49)**

#### **Day 46-47: gendung.rs - 地牢生成核心**

**文件**: `src/levels/gendung.rs`
**依赖**: `src/levels/types.rs`
**预计行数**: ~1,000 lines
**测试**: 15 tests

**任务清单**:

```rust
// 1. 核心数据结构 (200 lines)
pub struct Dungeon {
    pub tiles: [[u8; MAXDUNY]; MAXDUNX],        // dungeon[]
    pub flags: [[DungeonFlags; MAXDUNY]; MAXDUNX], // dFlags[]
    pub player_layer: [[u8; MAXDUNY]; MAXDUNX],    // dPlayer[]
    pub monster_layer: [[u8; MAXDUNY]; MAXDUNX],   // dMonster[]
    pub object_layer: [[u8; MAXDUNY]; MAXDUNX],    // dObject[]
    pub item_layer: [[u8; MAXDUNY]; MAXDUNX],      // dItem[]
    pub piece: [[i16; MAXDUNY]; MAXDUNX],          // dPiece[]
    pub trans_val: [[i8; MAXDUNY]; MAXDUNX],       // dTransVal[]
}

bitflags! {
    pub struct DungeonFlags: u8 {
        const POPULATED = 0x01;
        const MISSILE = 0x02;
        const VISIBLE = 0x04;
        const DEAD_PLAYER = 0x08;
        const BFLAG_LIT = 0x10;
        const BFLAG_EXPLORED = 0x20;
    }
}

// 2. 初始化函数 (150 lines)
impl Dungeon {
    pub fn new() -> Self { ... }
    pub fn reset(&mut self) { ... }
    pub fn init_dungeon_flags(&mut self) { ... }
}

// 3. 瓦片操作 (250 lines)
impl Dungeon {
    pub fn set_piece(&mut self, x: i32, y: i32, piece: i16) { ... }
    pub fn get_piece(&self, x: i32, y: i32) -> i16 { ... }
    pub fn make_tile_walkable(&mut self, x: i32, y: i32) { ... }
    pub fn make_tile_solid(&mut self, x: i32, y: i32) { ... }
    pub fn is_tile_walkable(&self, x: i32, y: i32) -> bool { ... }
    pub fn is_tile_solid(&self, x: i32, y: i32) -> bool { ... }
}

// 4. 主生成流程 (400 lines)
impl Dungeon {
    pub fn generate_level(&mut self, level_type: LevelType, entry: i32) { ... }
    pub fn create_dungeon(&mut self, seed: u32, entry: i32) { ... }
    pub fn load_dungeon_base(&mut self, level_type: LevelType) { ... }
    pub fn fix_dungeon_edges(&mut self) { ... }
    pub fn fix_tile_patterns(&mut self) { ... }
    pub fn init_dungeon_pieces(&mut self) { ... }
}
```

**测试覆盖** (15 tests):
- `test_dungeon_new()` - 初始化
- `test_reset_dungeon()` - 重置
- `test_set_get_piece()` - 瓦片设置/获取
- `test_make_tile_walkable()` - 可行走瓦片
- `test_make_tile_solid()` - 实心瓦片
- `test_is_tile_walkable()` - 行走检测
- `test_dungeon_flags()` - 标记系统
- `test_init_dungeon_flags()` - 标记初始化
- `test_bounds_checking()` - 边界检查
- `test_fix_dungeon_edges()` - 边缘修复
- `test_tile_patterns()` - 瓦片模式
- `test_layer_operations()` - 层操作
- `test_trans_val()` - 透明度
- `test_dungeon_coordinates()` - 坐标转换
- `test_dungeon_size_constants()` - 尺寸常量

**C++ 对齐**:
- ✅ `dungeon[]` → `Dungeon::tiles`
- ✅ `dFlags[]` → `Dungeon::flags`
- ✅ `dPiece[]` → `Dungeon::piece`
- ✅ `InitDungeon()` → `Dungeon::init_dungeon_flags()`
- ✅ `MakeDungeon()` → `Dungeon::create_dungeon()`

---

#### **Day 48-49: tile_properties.rs - 瓦片属性系统**

**文件**: `src/levels/tile_properties.rs`
**依赖**: `gendung.rs`
**预计行数**: ~300 lines
**测试**: 8 tests

**任务清单**:

```rust
// 1. 瓦片属性枚举 (100 lines)
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TileProperty {
    None = 0,
    Solid = 1,          // 实心墙
    Transparent = 2,     // 透明
    Trap = 3,           // 陷阱
    MissileBlock = 4,   // 阻挡导弹
}

bitflags! {
    pub struct TileFlags: u16 {
        const SOLID = 0x0001;
        const BLOCK_LIGHT = 0x0002;
        const BLOCK_PLAYER = 0x0004;
        const BLOCK_MONSTER = 0x0008;
        const BLOCK_MISSILE = 0x0010;
        const TRAP = 0x0020;
        const SPECIAL = 0x0040;
        const TRANSPARENT = 0x0080;
    }
}

// 2. 瓦片属性表 (100 lines)
pub struct TilePropertyManager {
    properties: [TileFlags; MAXTILES],
}

impl TilePropertyManager {
    pub fn new() -> Self { ... }
    pub fn load_tile_properties(&mut self, level_type: LevelType) { ... }
    pub fn get_tile_flags(&self, tile_id: u16) -> TileFlags { ... }
    pub fn set_tile_flags(&mut self, tile_id: u16, flags: TileFlags) { ... }
    pub fn is_tile_solid(&self, tile_id: u16) -> bool { ... }
    pub fn blocks_light(&self, tile_id: u16) -> bool { ... }
    pub fn blocks_missile(&self, tile_id: u16) -> bool { ... }
}

// 3. 瓦片查询函数 (100 lines)
impl TilePropertyManager {
    pub fn is_walkable(&self, tile_id: u16) -> bool { ... }
    pub fn is_door(&self, tile_id: u16) -> bool { ... }
    pub fn is_arch(&self, tile_id: u16) -> bool { ... }
    pub fn is_stairs(&self, tile_id: u16) -> bool { ... }
}
```

**测试覆盖** (8 tests):
- `test_tile_properties_new()`
- `test_load_tile_properties()`
- `test_get_tile_flags()`
- `test_is_tile_solid()`
- `test_blocks_light()`
- `test_blocks_missile()`
- `test_is_walkable()`
- `test_tile_query_functions()`

---

### 📅 **Phase 2: Cathedral Generation (Day 50-52)**

#### **Day 50-52: drlg_l1.rs - Cathedral (教堂层) 生成**

**文件**: `src/levels/drlg_l1.rs`
**依赖**: `gendung.rs`, `tile_properties.rs`
**预计行数**: ~1,500 lines
**测试**: 20 tests

**C++ 源码** (Source/levels/drlg_l1.cpp - 1,220 lines):
```cpp
// 主要函数:
void CreateL1Dungeon(uint32_t rseed, lvl_entry entry);
void DRLG_L1SetRoom(int rx1, int ry1);
void DRLG_L1Shadows();
void DRLG_L1Floor();
void DRLG_L1Pass3();
```

**任务清单**:

```rust
// 1. Cathedral 房间类型 (200 lines)
#[derive(Debug, Clone, Copy)]
pub enum L1RoomType {
    None = 0,
    Normal = 1,      // 普通房间
    HallHorizontal = 2, // 水平走廊
    HallVertical = 3,   // 垂直走廊
    Corner = 4,      // 转角
    Cross = 5,       // 十字路口
}

pub struct L1Room {
    pub x: i32,
    pub y: i32,
    pub width: i32,
    pub height: i32,
    pub room_type: L1RoomType,
    pub doors: Vec<Point>,
}

// 2. Cathedral 生成器 (400 lines)
pub struct L1Generator {
    dungeon: Dungeon,
    rooms: Vec<L1Room>,
    rng: Rng,
    room_count: usize,
}

impl L1Generator {
    pub fn new(seed: u32) -> Self { ... }

    // 主生成流程
    pub fn create_l1_dungeon(&mut self, entry: LevelEntry) { ... }

    // Pass 1: 房间布局
    pub fn generate_room_layout(&mut self) { ... }
    pub fn place_room(&mut self, x: i32, y: i32, room_type: L1RoomType) -> bool { ... }
    pub fn place_corridors(&mut self) { ... }

    // Pass 2: 连接房间
    pub fn connect_rooms(&mut self) { ... }
    pub fn create_door(&mut self, x: i32, y: i32) { ... }

    // Pass 3: 填充细节
    pub fn fill_room_details(&mut self) { ... }
    pub fn place_pillars(&mut self) { ... }
}

// 3. Cathedral 瓦片放置 (500 lines)
impl L1Generator {
    pub fn set_floor_tile(&mut self, x: i32, y: i32) { ... }
    pub fn set_wall_tile(&mut self, x: i32, y: i32, direction: Direction) { ... }
    pub fn place_shadows(&mut self) { ... }
    pub fn place_arches(&mut self) { ... }
    pub fn fix_wall_patterns(&mut self) { ... }
}

// 4. Cathedral 特殊功能 (400 lines)
impl L1Generator {
    pub fn place_stairs_up(&mut self) { ... }
    pub fn place_stairs_down(&mut self) { ... }
    pub fn create_chamber(&mut self, x: i32, y: i32, width: i32, height: i32) { ... }
    pub fn is_room_valid(&self, x: i32, y: i32, width: i32, height: i32) -> bool { ... }
}
```

**测试覆盖** (20 tests):
- `test_l1_generator_new()` - 初始化
- `test_create_l1_dungeon()` - 完整生成
- `test_generate_room_layout()` - 房间布局
- `test_place_room()` - 房间放置
- `test_place_corridors()` - 走廊放置
- `test_connect_rooms()` - 房间连接
- `test_create_door()` - 门创建
- `test_fill_room_details()` - 细节填充
- `test_place_pillars()` - 柱子放置
- `test_set_floor_tile()` - 地板瓦片
- `test_set_wall_tile()` - 墙壁瓦片
- `test_place_shadows()` - 阴影放置
- `test_place_arches()` - 拱门放置
- `test_fix_wall_patterns()` - 墙壁模式修复
- `test_place_stairs_up()` - 上楼梯
- `test_place_stairs_down()` - 下楼梯
- `test_create_chamber()` - 房间创建
- `test_is_room_valid()` - 房间验证
- `test_l1_deterministic()` - 确定性测试 (相同种子→相同地图)
- `test_l1_complete_dungeon()` - 完整地牢测试

**C++ 对齐**:
- ✅ `CreateL1Dungeon()` → `L1Generator::create_l1_dungeon()`
- ✅ `DRLG_L1SetRoom()` → `L1Generator::place_room()`
- ✅ `DRLG_L1Shadows()` → `L1Generator::place_shadows()`
- ✅ `DRLG_L1Floor()` → `L1Generator::set_floor_tile()`
- ✅ `DRLG_L1Pass3()` → `L1Generator::fill_room_details()`

---

### 📅 **Phase 3: Town & Triggers (Day 53-55)**

#### **Day 53-54: town.rs - 城镇生成**

**文件**: `src/levels/town.rs`
**依赖**: `gendung.rs`
**预计行数**: ~500 lines
**测试**: 10 tests

**C++ 源码** (Source/levels/town.cpp - 354 lines):
```cpp
void CreateTown(int entry);
void LoadTownObjects();
void InitTownTriggers();
```

**任务清单**:

```rust
// 1. 城镇数据结构 (100 lines)
pub struct Town {
    dungeon: Dungeon,
    entry_point: Point,
    buildings: Vec<Building>,
}

pub struct Building {
    pub x: i32,
    pub y: i32,
    pub width: i32,
    pub height: i32,
    pub building_type: BuildingType,
}

#[derive(Debug, Clone, Copy)]
pub enum BuildingType {
    Smithy,          // 铁匠铺
    Tavern,          // 酒馆
    Church,          // 教堂
    Healer,          // 治疗师
    Witch,           // 女巫小屋
    MayorHouse,      // 市长的家
    StorytellerHouse, // 讲故事的人
}

// 2. 城镇生成 (300 lines)
impl Town {
    pub fn new() -> Self { ... }
    pub fn create_town(&mut self, entry: LevelEntry) { ... }
    pub fn load_town_layout(&mut self) { ... }
    pub fn place_buildings(&mut self) { ... }
    pub fn place_npcs(&mut self) { ... }
}

// 3. 城镇物品 (100 lines)
impl Town {
    pub fn load_town_objects(&mut self) { ... }
    pub fn place_fountain(&mut self, x: i32, y: i32) { ... }
    pub fn place_well(&mut self) { ... }
}
```

**测试覆盖** (10 tests):
- `test_town_new()`
- `test_create_town()`
- `test_load_town_layout()`
- `test_place_buildings()`
- `test_place_npcs()`
- `test_load_town_objects()`
- `test_place_fountain()`
- `test_place_well()`
- `test_town_entry_points()`
- `test_town_walkability()`

---

#### **Day 55: trigs.rs - 触发器系统**

**文件**: `src/levels/trigs.rs`
**依赖**: `gendung.rs`, `town.rs`
**预计行数**: ~1,000 lines
**测试**: 12 tests

**C++ 源码** (Source/levels/trigs.cpp - 856 lines):
```cpp
void InitTriggers();
void CheckTriggers();
void TriggerStairs(int tv);
```

**任务清单**:

```rust
// 1. 触发器类型 (200 lines)
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TriggerType {
    None = 0,
    StairsUp = 1,      // 上楼梯
    StairsDown = 2,    // 下楼梯
    Portal = 3,        // 传送门
    Shrine = 4,        // 神龛
    Fountain = 5,      // 喷泉
    Door = 6,          // 门
    Chest = 7,         // 宝箱
    Lever = 8,         // 拉杆
    PedistalOfBlood = 9, // 血之基座
}

pub struct Trigger {
    pub x: i32,
    pub y: i32,
    pub trigger_type: TriggerType,
    pub level: u8,       // 目标层数
    pub message: Option<String>,
    pub active: bool,
}

// 2. 触发器管理器 (400 lines)
pub struct TriggerManager {
    triggers: Vec<Trigger>,
    max_triggers: usize,
}

impl TriggerManager {
    pub fn new() -> Self { ... }
    pub fn init_triggers(&mut self) { ... }
    pub fn add_trigger(&mut self, trigger: Trigger) -> Result<()> { ... }
    pub fn remove_trigger(&mut self, index: usize) { ... }
    pub fn check_triggers(&mut self, player_pos: Point) -> Option<Trigger> { ... }
    pub fn trigger_stairs(&mut self, trigger_value: i32) { ... }
}

// 3. 特殊触发器 (400 lines)
impl TriggerManager {
    pub fn init_town_triggers(&mut self) { ... }
    pub fn init_dungeon_triggers(&mut self, level: u8) { ... }
    pub fn add_stairs_trigger(&mut self, x: i32, y: i32, is_up: bool, target_level: u8) { ... }
    pub fn add_portal_trigger(&mut self, x: i32, y: i32, target_level: u8) { ... }
    pub fn add_shrine_trigger(&mut self, x: i32, y: i32, shrine_type: u8) { ... }
}
```

**测试覆盖** (12 tests):
- `test_trigger_manager_new()`
- `test_add_trigger()`
- `test_remove_trigger()`
- `test_check_triggers()`
- `test_trigger_stairs()`
- `test_init_town_triggers()`
- `test_init_dungeon_triggers()`
- `test_add_stairs_trigger()`
- `test_add_portal_trigger()`
- `test_add_shrine_trigger()`
- `test_trigger_activation()`
- `test_trigger_max_limit()`

---

### 📅 **Phase 4: Advanced Generation (Day 56-58)**

#### **Day 56-57: themes.rs - 主题房间系统**

**文件**: `src/levels/themes.rs`
**依赖**: `gendung.rs`, `drlg_l1.rs`
**预计行数**: ~1,200 lines
**测试**: 15 tests

**C++ 源码** (Source/levels/themes.cpp - 890 lines):
```cpp
void InitThemes();
void CreateThemeRooms();
void GenerateThemeObjects();
```

**任务清单**:

```rust
// 1. 主题类型 (200 lines)
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ThemeType {
    None = 0,
    Barrel = 1,        // 桶房间
    Shrine = 2,        // 神龛房间
    MonstPit = 3,      // 怪物陷阱
    SkelRoom = 4,      // 骷髅房间
    Treasure = 5,      // 宝藏房间
    Library = 6,       // 图书馆
    Torture = 7,       // 酷刑室
    BloodFount = 8,    // 血泉
    Decap = 9,         // 断头台
    PurifyingFount = 10, // 净化泉
    ArmorStand = 11,   // 盔甲架
    GoatShrine = 12,   // 山羊神龛
    Cauldron = 13,     // 大锅
    MurkyPool = 14,    // 浑浊水池
    TearFountain = 15, // 泪泉
}

pub struct ThemeRoom {
    pub x: i32,
    pub y: i32,
    pub width: i32,
    pub height: i32,
    pub theme_type: ThemeType,
    pub objects: Vec<Point>,
}

// 2. 主题管理器 (500 lines)
pub struct ThemeManager {
    themes: Vec<ThemeRoom>,
    dungeon: Dungeon,
    rng: Rng,
}

impl ThemeManager {
    pub fn new(seed: u32) -> Self { ... }
    pub fn init_themes(&mut self) { ... }
    pub fn create_theme_rooms(&mut self, room_count: usize) { ... }
    pub fn find_theme_location(&self) -> Option<Point> { ... }
    pub fn place_theme_room(&mut self, x: i32, y: i32, theme_type: ThemeType) { ... }
}

// 3. 主题生成 (500 lines)
impl ThemeManager {
    pub fn generate_barrel_room(&mut self, room: &ThemeRoom) { ... }
    pub fn generate_shrine_room(&mut self, room: &ThemeRoom) { ... }
    pub fn generate_monster_pit(&mut self, room: &ThemeRoom) { ... }
    pub fn generate_treasure_room(&mut self, room: &ThemeRoom) { ... }
    pub fn generate_library(&mut self, room: &ThemeRoom) { ... }
    pub fn generate_torture_room(&mut self, room: &ThemeRoom) { ... }
    pub fn generate_theme_objects(&mut self, room: &ThemeRoom) { ... }
}
```

**测试覆盖** (15 tests):
- `test_theme_manager_new()`
- `test_init_themes()`
- `test_create_theme_rooms()`
- `test_find_theme_location()`
- `test_place_theme_room()`
- `test_generate_barrel_room()`
- `test_generate_shrine_room()`
- `test_generate_monster_pit()`
- `test_generate_treasure_room()`
- `test_generate_library()`
- `test_generate_torture_room()`
- `test_generate_theme_objects()`
- `test_theme_room_constraints()`
- `test_theme_random_selection()`
- `test_theme_deterministic()`

---

#### **Day 58: setmaps.rs - Quest 特殊地图**

**文件**: `src/levels/setmaps.rs`
**依赖**: `gendung.rs`
**预计行数**: ~400 lines
**测试**: 8 tests

**C++ 源码** (Source/levels/setmaps.cpp - 156 lines):
```cpp
void LoadPresetMap(int setpc_type, int setpc_x, int setpc_y);
```

**任务清单**:

```rust
// 1. 预设地图类型 (100 lines)
#[derive(Debug, Clone, Copy)]
pub enum PresetMapType {
    ButcherRoom = 1,   // 屠夫房间
    SkelKingRoom = 2,  // 骷髅王房间
    PoisonWaterRoom = 3, // 毒水房间
    ChamberOfBone = 4,  // 骨之房间
}

pub struct PresetMap {
    pub map_type: PresetMapType,
    pub width: i32,
    pub height: i32,
    pub tiles: Vec<Vec<u8>>,
}

// 2. 预设地图加载 (200 lines)
pub struct SetMapLoader {
    maps: HashMap<PresetMapType, PresetMap>,
}

impl SetMapLoader {
    pub fn new() -> Self { ... }
    pub fn load_preset_map(&mut self, map_type: PresetMapType) -> Result<PresetMap> { ... }
    pub fn place_preset_map(&mut self, dungeon: &mut Dungeon, x: i32, y: i32, map: &PresetMap) { ... }
}

// 3. Quest 地图集成 (100 lines)
impl SetMapLoader {
    pub fn init_butcher_room(&mut self, dungeon: &mut Dungeon) { ... }
    pub fn init_skel_king_room(&mut self, dungeon: &mut Dungeon) { ... }
    pub fn init_poison_water_room(&mut self, dungeon: &mut Dungeon) { ... }
}
```

**测试覆盖** (8 tests):
- `test_preset_map_new()`
- `test_load_preset_map()`
- `test_place_preset_map()`
- `test_init_butcher_room()`
- `test_init_skel_king_room()`
- `test_init_poison_water_room()`
- `test_preset_map_bounds()`
- `test_preset_map_tiles()`

---

### 📅 **Phase 5: Integration & Testing (Day 59-60)**

#### **Day 59: Level Manager - 关卡管理器**

**文件**: `src/levels/mod.rs`, `src/levels/level_manager.rs`
**依赖**: 所有 levels 模块
**预计行数**: ~600 lines
**测试**: 12 tests

**任务清单**:

```rust
// 1. Level Manager (400 lines)
pub struct LevelManager {
    current_level: u8,
    dungeon: Dungeon,
    l1_generator: Option<L1Generator>,
    town: Option<Town>,
    trigger_manager: TriggerManager,
    theme_manager: Option<ThemeManager>,
}

impl LevelManager {
    pub fn new() -> Self { ... }

    // 主接口
    pub fn load_level(&mut self, level: u8, entry: LevelEntry) -> Result<()> { ... }
    pub fn generate_level(&mut self, level: u8, seed: u32) -> Result<()> { ... }
    pub fn get_dungeon(&self) -> &Dungeon { ... }
    pub fn get_trigger_manager(&mut self) -> &mut TriggerManager { ... }

    // 关卡类型判断
    pub fn is_town(&self, level: u8) -> bool { ... }
    pub fn get_level_type(&self, level: u8) -> LevelType { ... }
}

// 2. Level Type (200 lines)
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum LevelType {
    Town = 0,
    Cathedral = 1,     // L1-L4
    Catacombs = 2,     // L5-L8
    Caves = 3,         // L9-L12
    Hell = 4,          // L13-L16
}

#[derive(Debug, Clone, Copy)]
pub enum LevelEntry {
    MainEntry = 0,
    StairsUp = 1,
    StairsDown = 2,
    Portal = 3,
}
```

**测试覆盖** (12 tests):
- `test_level_manager_new()`
- `test_load_level_town()`
- `test_load_level_cathedral()`
- `test_generate_level()`
- `test_get_dungeon()`
- `test_get_trigger_manager()`
- `test_is_town()`
- `test_get_level_type()`
- `test_level_transition()`
- `test_level_entry_points()`
- `test_level_deterministic()`
- `test_level_complete_generation()`

---

#### **Day 60: Integration Testing & Documentation**

**任务**:
1. **集成测试** (200 lines):
   - 完整地牢生成测试 (Town → L1 → L2)
   - 触发器交互测试
   - 主题房间生成测试
   - 性能基准测试

2. **文档完善**:
   - 更新 `00.PortingStatus.md`
   - 创建 M6 完成报告
   - 添加 API 文档

3. **运行全套测试**:
   ```bash
   cargo test --all
   cargo test --test integration_level_generation
   cargo bench --bench level_generation
   ```

**预期测试结果**:
- 单元测试: ~100 tests (Day 46-60)
- 总测试: ~486 tests (之前 386 + 新增 100)
- 通过率: 100%

---

## 📦 模块结构

```
src/levels/
├── mod.rs                  // 模块导出
├── types.rs                // 通用类型 (LevelType, LevelEntry, Direction)
├── gendung.rs              // 地牢生成核心 (1,000 lines) ⭐⭐⭐
├── tile_properties.rs      // 瓦片属性 (300 lines) ⭐⭐
├── drlg_l1.rs              // Cathedral 生成 (1,500 lines) ⭐⭐⭐
├── town.rs                 // 城镇生成 (500 lines) ⭐⭐
├── trigs.rs                // 触发器系统 (1,000 lines) ⭐⭐
├── themes.rs               // 主题房间 (1,200 lines) ⭐
├── setmaps.rs              // Quest 地图 (400 lines) ⚠️
└── level_manager.rs        // 关卡管理器 (600 lines) ⭐⭐

Total: ~6,500 lines (不含测试)
Tests: ~100 tests
```

---

## 🎯 M6 验收标准

### 功能要求:
- ✅ 可生成 Tristram (城镇)
- ✅ 可生成 Cathedral L1 (教堂第1层)
- ✅ 楼梯/传送门触发器工作正常
- ✅ 主题房间可随机生成
- ✅ 地图每次生成确定性 (相同种子 → 相同地图)
- ✅ 所有瓦片可行走性正确
- ✅ 门、墙、地板渲染正确

### 测试要求:
- ✅ 100 单元测试通过
- ✅ 集成测试通过
- ✅ 性能: 生成单层 < 100ms

### 可玩性:
- ✅ 玩家可在城镇移动
- ✅ 玩家可通过楼梯进入 L1
- ✅ L1 地图完整可行走
- ✅ 怪物可在 L1 生成 (需 M5 Monster System)
- ✅ 物品可在 L1 生成 (需 Items)

---

## 📊 进度追踪

| Day | 模块 | 行数 | 测试 | 状态 |
|-----|------|------|------|------|
| 46-47 | gendung.rs | 1,000 | 15 | ⏳ 待开始 |
| 48-49 | tile_properties.rs | 300 | 8 | ⏳ 待开始 |
| 50-52 | drlg_l1.rs | 1,500 | 20 | ⏳ 待开始 |
| 53-54 | town.rs | 500 | 10 | ⏳ 待开始 |
| 55 | trigs.rs | 1,000 | 12 | ⏳ 待开始 |
| 56-57 | themes.rs | 1,200 | 15 | ⏳ 待开始 |
| 58 | setmaps.rs | 400 | 8 | ⏳ 待开始 |
| 59 | level_manager.rs | 600 | 12 | ⏳ 待开始 |
| 60 | Integration & Tests | - | - | ⏳ 待开始 |

**总计**: ~6,500 lines, ~100 tests

---

## 🔗 依赖关系

```
gendung.rs (核心)
    ↓
tile_properties.rs
    ↓
drlg_l1.rs ← themes.rs
    ↓         ↓
town.rs → trigs.rs → setmaps.rs
    ↓
level_manager.rs (整合)
```

---

## 📝 备注

### 简化策略:
1. **M6 仅实现 Cathedral L1** (不实现 L2/L3/L4)
2. **暂不实现**: drlg_l2.rs, drlg_l3.rs, drlg_l4.rs, crypt.rs
3. **Quest 地图**: 仅实现基础框架，不实现完整 Quest 逻辑
4. **主题房间**: 实现核心系统，部分主题类型可留空

### 后续扩展 (M7+):
- drlg_l2.rs (Catacombs)
- drlg_l3.rs (Caves)
- drlg_l4.rs (Hell)
- crypt.rs (特殊区域)
- 完整 Quest 系统
- 更多主题房间类型

---

## 🎉 Day 60 完成标准

**M6 Complete Checklist**:
- ✅ 所有模块文件创建
- ✅ 100 单元测试通过
- ✅ 集成测试通过
- ✅ 可生成 Town + Cathedral L1
- ✅ 触发器系统工作
- ✅ 主题房间可生成
- ✅ 文档完整
- ✅ 性能达标

**庆祝时刻**: 🎊 **第一个可玩的地牢层完成！**
