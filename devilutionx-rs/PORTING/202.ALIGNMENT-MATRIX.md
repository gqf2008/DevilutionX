# DevilutionX-RS ↔ C++ 对齐矩阵

**用途**: 全部对齐工作流的追踪文档。每个子系统一行，标注移植状态、证据与已知偏差；每完成一项即更新本表并跑通 `cargo test`。

**建立日期**: 2026-08-01
**C++ 基准**: **upstream/master @ `e10932e64`（2026-08-02 同步）**——本地 `Source/` 已整体同步到 upstream/master（27 个 C++ 提交/311 文件，+12435/−8325；含 `tl::expected`→`std::expected`（63e85d9b1）、libfmt→std::format（ac2d7fc73）、DOS→SDL3（1ca75e477）、Mushroom Quest 新状态（2c1a364da）、mod hashing/manifest 等）。**cpp-repros 已适配**：`tools/l4repro` 的 `LoadDungeonBase`/`LoadL4Dungeon` 签名改为 `std::expected<void,std::string>` 并在 CI 中升级 `-std=c++23`（本地 g++ 14 编译 + seed 717625719 冒烟通过）；l1trans_repro 自包含不受影响。历史：2026-08-01 审计 176 提交/310 C++ 文件；关键语义变更（`msg.h` CmdId、`options.h` 选项删除）已同步到 Rust
**Rust 分支**: `devilutionx-rs`（57 个移植提交，最后活动 2026-07-27）
**验证基线**: `cargo test` 全绿 — lib 2627 / bins 2566 / 集成 19 / doctests 19，0 失败（2026-08-02 本轮）

> 状态图例: ✅ 已移植并编译 · ⚠️ 部分移植 · ❌ 未移植（或仅空壳目录，未进入 `lib.rs`/`main.rs` 模块图）

---

## 1. 总览

| 指标 | 值 |
|---|---|
| C++ 总行数（Source/） | ~125,500 |
| Rust 编译模块行数 | ~162,000（含内联测试与多套重叠实现） |
| 目录镜像 | 18/18 C++ 目录在 `devilutionx-rs/src/` 有同名目录 |
| C++ 根 `.cpp`（~62 个） | 几乎全部有同名 `src/game/*.rs` 对应 |
| 未编译空壳目录 | DiabloUI, discord, dvlnet, items, lua, monsters, mpq, platform, portals, qol, quests, storm（约 2,800 行） |

## 2. 单机核心（高对齐 ✅）

| 子系统 | C++ 位置 | Rust 位置 | 证据 / 测试 | 已知偏差 |
|---|---|---|---|---|
| 数据表 | spelldat/monstdat/itemdat/playerdat/item_affix/objdat（~8k 行） |  | `game/{spelldat,monstdat,itemdat,item_affix,objdat,player_dat}.rs` | 数据条数对齐（52 spells/138 monsters/167 base+90 unique/178 affixes/109 objects）；TSV 位掩码对齐测试；**`rnd_all_items` 金分支修复（2026-08-02）**——C++ `RndAllItems`（items.cpp:1392）75% 返回 `IDI_GOLD`=0，原 Rust 占位返回 None；现返回 `Some(ItemId::Gold)`，新增 `test_rnd_all_items_gold_roll_returns_index_zero`；**掉落决策掷骰对齐 C++ RndItemForMonsterLevel（2026-08-02）**——`roll_monster_drop` 原为 `>=40`/`roll<70` 简化概率，现按 items.cpp:3240-3251 逐掷对齐：`GenerateRnd(100)>40` 无掉落、`>25` 金（74%）、否则物品；新增 `test_drop_rolls_match_cpp_probabilities`（2000 掷统计：掉落率 40%±、金 74%±、药水 26%±）；**真实物品掉落接入（2026-08-02）**——非金分支改用 `get_item_index_for_droppable` 按怪物等级从 ITEMS_DATA 选真实基物品，`GroundItem` 增 `item_index` 字段，击杀路径携带怪物等级，拾取按 index 查名；`test_drop_type_distribution` 更新为 C++ 分布（金 74%±、非金 26%±、真实物品 >100 次）；**ITEMS_DATA 重排为 itemdat.tsv 行序（2026-08-02）**——从 upstream/master 提取权威 `assets/txtdata/items/itemdat.tsv`（168 行），脚本化重生成 `ITEMS_DATA: [ItemData; 168]`（原 167 行按类别排序、字段值含猜测；C++ `AllItemsList` = TSV 行序，`_item_indexes` 枚举值 == TSV 行号，`GetItemAttrs` 直接 `AllItemsList[itemData]`（items.cpp:3117），`ItemMappingIdsToIndices` 为恒等映射）；`get_item_data(ItemId::X as usize)` 现返回正确条目（新增 `test_item_id_equals_tsv_row`：Heal=24→Potion of Healing、Mushroom=17→Black Mushroom、Anvil=16→Anvil of Fury）；`items.rs ItemIndex` 枚举 77 变体重映射到 TSV 行（原为经典 vanilla 索引 20/40/80/100…），`item_new.rs` 小枚举 Gold=68→0、ShortSword=0→119；短法杖充能测试修正（TSV row 151 miscId=Staff → 40 充能，与 C++ 一致）；**EXP_LEVELS 已用 Experience.tsv 权威验证**——Rust 50 值与 upstream `assets/txtdata/Experience.tsv` 逐值一致（Level 1=2000 确认无误）；生成器与 TSV 存于 `devilutionx-rs/tools/table_data/`（可复现）；**UNIQUE_ITEMS 重生成（2026-08-02）**——原 90 行表为作者自拟（顺序与数值均非 C++：如 index 2 写 Windforce 实为 Empyrean Band、The Butcher Cleaver value=3000 实为 3650）；现按 `unique_itemdat.tsv` 行序重生成全部 90 个唯一物品（name/cursor/uniqueBaseItem/minLevel/value/6 槽 powers + numPowers=连续非空槽数，与 C++ `LoadUniqueItemDatFromFile` 一致），`get_unique_item_data(UniqueItemId::X)` 现返回正确条目；6 个唯一物品测试改用 `UniqueItemId` + TSV 权威值（Windforce=WarBow/17/37750、Grandfather=GreatSword/27/119800、Veil of Steel=SteelVeil/63800、Arkaine's Valor=ArmorOfValor/42000、Ring of Truth=TRing/9100、Optic Amulet=OptAmulet/9750）；**SetupAllItems 完整物品生成（2026-08-02）**——`item_affix::setup_all_items` （items.cpp:3253-3298）补齐 GetItemAttrs（经新增 `items::get_item_attrs_by_index`，避免 ItemIndex 枚举未覆盖全部 TSV 行）、GetItemBLevel（含 GenerateRnd 掷骰 + Staff/Ring/Amulet 短路）、CheckUnique（GetValidUniques 按 uniqueBaseItem + DiscardRandomValues(1) + uidOffset 倒序选取）、GetUniqueItem（按 uid 应用 powers/名称/光标/价值/CF_UNIQUE）、ItemRndDur（`GenerateRnd(max/2)+max/4+1`）、quest-unique（IMISC_UNIQUE，iseed 作 uid，seed>109 或基物品不匹配则 clear）；`rnd_pl` 与 `get_item_attrs` 护甲掷骰改用 C++ 全局 LCG；`CreateInfo` 标志对齐 C++ 位值（CF_ONLYGOOD=1<<6…CF_PREGEN=1<<15）；**掉落接线（2026-08-02）**——`roll_monster_drop` 非金分支经 `setup_all_items` 生成真实 Item（种子取自调用方 RNG，`GroundItem` 增 `item` 字段并转 Clone），拾取直接显示生成后的名称/品质；`get_item_index_for_droppable` 改用 `consider_drop_rate=true`（对齐 C++ RndItemForMonsterLevel 按 iDropRate 加权）；新增 6 个确定性单测（create_info/CheckUnique/GetUniqueItem/quest-unique clear/ItemRndDur 分支/GetItemBLevel 短路）；**Staff 法术/充能链（2026-08-02）**——`get_staff_spell_cpp`（items.cpp:1237-1280 GetStaffSpell）完整移植：非 Hellfire 25% `FlipCoin(4)` 走 GetItemPower（staff affix），否则按 `GenerateRnd(SpellsData.size())` 从 Firebolt 扫描（单机跳过 Resurrect/HealOther、到 size 回绕），选定法术 → `staffMin + GenerateRnd(staffMax-staffMin+1)` 充能、`minInt` 需求、`charges*staffCost/5` 加值；`get_spell_staff_level`（spells.cpp:326-348，含 shareware 禁用表）对齐；`apply_staff_power`（GetStaffPower）补 `"{base} of {spell}"` / `"{prefix} {base} of {spell}"` 法杖命名；`get_item_bonus` staff 分支接入；新增 3 个测试（staff level 纯函数/法杖命名/充能范围不变式，46 个 item_affix 测试全绿） | — |
| 玩家战斗命中（combat_integration） | `Source/player.cpp`（PlrHitMonst） | `game/combat_integration.rs` | **玩家→怪物命中公式精确移植（2026-08-02）**：`calculate_player_to_hit` 原为简化式（50+lv/2+dex/4+weapon-ac）；现按 C++ `PlrHitMonst`（player.cpp:548-549）实现——`GetMeleePiercingToHit()`（level+dex/2+iBonusToHit+baseMeleeToHit+_pIEnAc，player.h:573/583）− `CalculateArmorPierce`（非 Hellfire：Barbarian 近战减 armor/8，player.h:653），clamp 5-95；新增 `test_calculate_player_to_hit_matches_cpp`（Warrior base=70、命中 80/92、边界 5/95）；**怪物→玩家命中公式精确移植（2026-08-02）**——`calculate_monster_to_hit` 原用 intelligence 代理；现按 C++ `MonsterAttackPlayer`（monster.cpp:1185-1189）：`toHit + 2*(mLevel-pLevel) + 30 - GetArmor()`，下限 `GetMinHit()`（L16=30/L15=25/L14=20/其他 15），`monster_attack_player` 增 `current_level` 参数；新增 `test_calculate_monster_to_hit_matches_cpp`（50、下限 15/30）；**怪物→玩家格挡投骰移植（2026-08-02）**——`monster_attack_player` 补 C++ `MonsterAttackPlayer`（monster.cpp:1191-1207）格挡：站立/攻击中且有盾时掷 `blk_roll`，`blk = dex + baseToBlock + 2*playerLevel - 2*monsterLevel`（clamp 0-100），命中后 `blk_roll < blk` → `StartPlrBlock`（`PlayerMode::Block`）并返回 `AttackResult::Block`；新增 `test_monster_attack_player_block_roll_matches_cpp`；**怪物→玩家伤害公式精确移植（2026-08-02）**——`monster_attack_player` 移除错误的 `armor_class/2` 减免（C++ `MonsterAttackPlayer` 无 AC 减免），按 monster.cpp:1219-1220 改为 `RandomIntBetween(min<<6,max<<6) + _pIGetHit<<6`，下限 64；新增 `test_monster_attack_player_damage_matches_cpp`（固定伤害 5+get_hit2 → 7 display，HP 精确 -448）；**玩家→怪物伤害公式精确移植（2026-08-02，ad1cacb38）**——`player_attack_monster` 移除错误的 `monster.armor_class/4` 减免（C++ `PlrHitMonst` 无 AC 减免，AC 只降命中）、补 `_pDamageMod`、暴击按 `PlayerClassFlag::CriticalStrike` 数据驱动（原硬编码 Warrior）；新增 `test_player_attack_monster_damage_matches_cpp`（5 伤害 → 64x=320 精确扣血）；**武器 vs 怪物种族伤害修正移植（2026-08-02，ad1cacb38 后续）**——`player_exact::PlayerItem` 增 `_itype`，`player_attack_monster` 按 C++ `PlrHitMonst`（player.cpp:581-609）实现剑/锤 vs Undead/Animal ±50% 修正（Demon TripleDemonDamage 待物品效果系统）；新增 `test_player_weapon_vs_monster_class_modifiers_match_cpp`（剑对僵尸 10→5、锤 10→15） | ⚠️ 怪物类型枚举仍为 20 类简化集（非完整 `_monster_id` 138 型）；**MONSTERS_DATA 全表重生成（2026-08-02）**——核实发现 monstdat.rs 138 行表中 **119 行数值来自旧版 TSV**（如 MT_INVILORD lvl=6/hp24-36 vs 现行 14/278-278、MT_LRDSAYTR 名 Lord Sayter vs Satyr Lord、Skeleton King hp240 vs 140、Diablo lvl45/ac145/exp33300 vs 30/90/31666、Fiend ai=Succubus vs Bat）；已按 upstream `monstdat_hf.tsv`（base 112 + Hellfire 26 = 138 行）重生成，19 字段（name/层级/hp/ai/int/toHit/damage/特殊伤害/ac/class/resistance/resHell/exp）逐值 0 偏差；6 个测试改用 TSV 权威值（SkeletonKing 9/140/70/570、Butcher 1/320/50/710、Diablo The Dark Lord 30/1666/90/31666、Golem 12/1/1、NaKrul 40/1332/13333、Fiend ai=Bat）；**基础数值已对齐 monstdat.tsv（2026-08-02）**——`monster.rs::base_stats` 原为自拟值（Zombie hp=8/exp=50），现按 upstream `monstdat.tsv` 权威行替换（hp=hitPointsMaximum + minDamage/maxDamage/armorClass/toHit/exp），每个简化类型标注对应 C++ `_monster_id`（Zombie→MT_NZOMBIE、Skeleton→MT_WSKELAX、Butcher→MT_CLEAVER、Diablo→MT_DIABLO 等；VenomSpitter→MT_RACID、VileOne→MT_HOLOWONE、MageHell→MT_COUNSLR 为最近似映射；Lazarus 无基础 TSV 行，保留演示近似）；新增 `test_base_stats_match_monstdat_tsv`（Zombie 7/2/5/5/10/54、Butcher 320/6/12/50/50/710、Diablo 1666/30/60/90/220/31666、Skeleton 4/1/4/0/20/64）；**怪物生成 HP 对齐 C++ InitMonster（2026-08-02）**——原 `Monster::new` 用 `hpMax*level_scale`（非 64x 定点且按层缩放），C++ 为 `max(RandomIntBetween(hpMin,hpMax) << 6 / 2, 64)`（monster.cpp:208-212，单人减半下限 64=1HP）；修复：`MonsterStats` 增 `hp_min`（20 型按 monstdat hitPointsMinimum），`Monster::new` 用上限 `hpMax<<5`（确定路径），新增 `Monster::new_with_rng` 掷区间，`place_dungeon_monsters` 接入真实掷骰——怪物不再一击死（Zombie 64x HP 128-224）；新增 `test_monster_hp_matches_cpp_init_monster`（上限 224、掷骰 128-224、Golem 下限 64）新增 `test_base_stats_match_monstdat_tsv`（Zombie 7/2/5/5/10/54、Butcher 320/6/12/50/50/710、Diablo 1666/30/60/90/220/31666、Skeleton 4/1/4/0/20/64）；**怪物生成 HP 对齐 C++ InitMonster（2026-08-02）**——原 `Monster::new` 用 `hpMax*level_scale`（非 64x 定点且按层缩放），C++ 为 `max(RandomIntBetween(hpMin,hpMax) << 6 / 2, 64)`（monster.cpp:208-212，单人减半下限 64=1HP）；修复：`MonsterStats` 增 `hp_min`（20 型按 monstdat hitPointsMinimum），`Monster::new` 用上限 `hpMax<<5`（确定路径），新增 `Monster::new_with_rng` 掷区间，`place_dungeon_monsters` 接入真实掷骰——怪物不再一击死（Zombie 64x HP 128-224）；新增 `test_monster_hp_matches_cpp_init_monster`（上限 224、掷骰 128-224、Golem 下限 64）；**其余数据表 TSV 验证（2026-08-02）**——objdat（109 行：ObjectId 顺序 = TSV id 列；**全表 109 行程序化验证 0 实差**——ofindex（file 去重）、min/maxLevel、levelType、theme、quest、animDelay/Len/Width 全吻合，flags 仅大小写命名差异（ANIMATED vs Animated），校验脚本 tools/table_data/check_objdat.py）、item affix（83 前缀 + 95 后缀 = 178；**全表程序化验证 0 实差**——name/minLevel/chance/power 类型与参数全吻合，check_affixes.py）；**GetItemIndexForDroppableItem 实装（2026-08-02）**——原为占位（返回 None + 伪代码注释），现按 items.cpp:1353-1377 在权威 ITEMS_DATA（168 行）上实现：`is_item_available`（items.cpp:2375-2403 非 Hellfire 过滤：quest 锁 22/32/5、Hellfire 专属 35-47/83-86/92/161-165）+ dropRate>0 + 单人排除 Resurrect/HealOther + 用户过滤，累积权重随机选（`random_range(0..cumulative)` 对应 `RandomIntLessThan`）；`rnd_all_items`/`rnd_u_item`/`rnd_type_items` 改收 rng + 游戏模式参数（原用全局 LCG `generate_rnd`）；新增 `test_get_item_index_for_droppable_picks_from_items_data`（L1 只出 min_mlvl<=1、金为候选）、spelldat（36 基础 + 15 Hellfire + Null = 52，SpellID 顺序与 TSV id 列一致）；**SPELLS_DATA 重生成（2026-08-02）**——全量比对发现 **49/52 行数值来自旧 TSV**（Lightning bookCost10 200→300、Flash staffCost10 37→50、Identify/Infravision 书成本 10/100→0（无书）等），已按 spelldat_hf.tsv 全 15 字段（成本/等级/minInt/missiles/manaMultiplier/minMana/staffMin/Max）重生成并 0 偏差（校验脚本 tools/table_data/check_spelldat.py）；对应 TSV（spelldat/objdat/item_prefixes/item_suffixes/classdat/towners）已提取至 `tools/table_data/` |
| 关卡生成 | `levels/`（12,161 行） | `levels/`（12,038 行） | drlg_l1–l4、town、gendung、themes、trigs、setmaps、tile_properties 全移植；确定性 PRNG 测试；**L3/L4 生成实证（2026-08-01）**：L3 全 1600 格有值（含楼梯 46–66；trans=0 与 C++ 一致——C++ L3 无 FloodTransparencyValues）、L4 全 1500+ 有值 + trans 2768–3232（C++ L4 flood floor_id=6）；**L2 空网格缺陷已修复 ✅**（根因 = room_id 0-based 与 C++ 1-based 错位：`nRid = nRoomCnt` 在 DefineRoom 自增之后，Rust 却取自增前 → 根的直接子房间 dest_room=0 → 其 hall（含 RNG 抽签+走廊挖掘）被整个跳过 → RNG 流发散 + 无走廊 → fill_voids 无法收敛。9a87f4ecf）。**用独立 C++ 复现程序逐格比对验证**：seed 1 的 8 个房间位置与 RNG 状态与 C++ 完全一致；generate() 现在每个测试种子产出 1520–1527 非零 tile（原 9）。配套修复：**FillVoid 完整移植**（f94f214e0）、**CreateRoom 抽签顺序对齐**（f94f214e0）。注：带 quest 房时 create_dungeon 成功率为 ~2%（C++ 亦然——C++ 参考程序同样失败于 seed 1，post-fill 754>700），generate 的 100 次重试覆盖之；**L1 已完全对齐（2026-08-01）**：Tile 枚举全面重对齐 C++ drlg_l1.cpp 值（含 fence/stair/shadow/dirt 全部变体）、minisets 按 C++ 重写（STAIRSUP/STAIRSDOWN/LAMPS/PWATERIN + L5STAIRSUP 原始大教堂楼梯）、完整移植 AddWall/HorizontalWall/VerticalWall/WallOk、PlaceMiniSet/PlaceStairs/重试循环、FixDirtTiles/FixCornerTiles/Substitution/ApplyShadowsPatterns（含 BaseTypes/TileDecorations/ShadowPatterns 表）——fixture 1-743271966 **逐格 1600/1600（100%）**；**L1–L4 渲染布局桥**（build_dungeon_layout + stamp_dungeon_layout（bg 7/11/7/29）+ generate_dungeon_layout 分派器）；**游戏流程已接入 L2–L4**（dungeon_art 缓存 + current_dungeon_level + descend_to_level，347d93856）；**楼梯触发器多层切换**（d2a3ce3ec）；**L2–L4 楼梯 miniset 与 C++ 逐值一致**（USTAIRS 72/77/76、DSTAIRS 48/71/50/78、WARPSTAIRS 158/160/159），**触发扫描常量对齐**（L2: 266/558/270；L3: 170/167；L4: 82/119），check_stairs_transition 用触发器表驱动全部 L1–L4 升降/回城；`tests/l2_stairs_flow.rs` 用真实 MPQ 验证楼梯触发 dPiece（shareware 无 l2data 时自跳过）；**L3 熔岩静态光 dPreLight 已烘焙并作渲染基底**（PlaceCaveLights + memcpy(dLight,dPreLight)，63a0202b7）；**DungeonType 枚举值对齐 C++**（Nest=5/Crypt=6，c45c1922b）；**L2 FloodTransparencyValues(3) 已按 C++ 顺序接入**（e7ab941dc）；**LevelEntry 对齐 C++ `lvl_entry`（8 值 Main..TownWarpUp）**、**TileProperties 位布局对齐 dun_tile.hpp**（修正 BLOCK_PLAYER/MONSTER 幻影位、Trap=1<<7、Transparent/TransparentLeft/Right，9f34a5818），且 **L3 L9 城镇传送门 miniset 改为无条件放置**（与 C++ `if (currlevel == 9)` 一致，9f34a5818）；**L3 CanReplaceTile 边界判定对齐 C++ drlg_l3.cpp:1367**（NW 在界 && dungeon[NW]<=100 && 任一 NW/SE/SW/NE>=84 → 不可替换；原 Rust 为整 3x3 邻域检查） | L1 tile→mega 映射为经验值；crypt（840 行）未移植；L2–L4 楼梯检测与熔岩光需真实完整版美术验证（shareware 无 L2–L4）；L1/L2/L4 静态光（火盆等）未生成；**L2 无 quest 房布局仍不收敛**（Q_BLOOD 未激活时 create_dungeon 失败——C++ 可生成，剩余房间树/fill_voids 差异待查） |
| 引擎渲染 | `engine/render/` scrollrt/dun_render/clx/palette/lighting/text + `vision.cpp` | `engine/{scrollrt,dun_render,clx,palette,lighting,text_render,...}.rs`、`game/lighting.rs` | 行号级移植（scrollrt.cpp:1573/652/927/521 等）；真实数据渲染 smoke 测试；**DoVision 射线算法已移植**（VisionRays 表 + 四象限镜像 + 对角线遮挡，`cast_vision_rays`）；**透明/fog 已接入渲染**（`dTransVal`/`TransList` + DoVision 射线，`Lighting::with_transparency`）；**foliage 已移植**（RenderTileFoliage/GetDunFrameFoliage，776defcf0）；**光照墙遮挡（2026-08-02）**——`DoVision` 射线止于 BlockLight 墙（SOL 数据），渲染管线现用 `cast_vision_rays` 结果掩蔽 dLight：射线不可达的 tile 置全暗（`apply_light_occlusion`），玩家光晕不再穿透墙壁；新增 2 测试（掩蔽纯函数 + 东墙遮挡集成，墙后 tile 全暗/墙前保持照亮）；**dPreLight 已作渲染基底**（L3 熔岩/L1 圣火/L2 火盆静态光烘焙进 `pre_light`） | 注：C++ 快照**无** MakeLight（径向 DoLighting + 射线 DoVision + dPreLight + 多光源 ProcessLightList 才是真实机制）。**实体入面（怪物，2026-08-02）**——`MonsterSprite` 增存 CLX 帧（索引色），`render_world_pipeline` 在 `draw_view` 之后、调色板上传之前把怪物用 `clx_draw`（C++ `RenderCl2Sprite` + 该 tile 光照表）绘入 8-bit 后备缓冲，深度按 wx+wy 排序；canvas 怪物通道只画无帧的彩色块回退；`Lighting::table_for` 转 pub。玩家精灵入面（2026-08-02）——`PlayerSprite` 增存 CLX 帧（main.rs 加载时重建），世界管线闭包在怪物之后按玩家 tile + 光照表绘入调色板面（C++ DrawView 中玩家在实体内）；canvas 通道在管线已跑且帧存在时跳过（回退路径仍用 RGBA/标记）。**多光源 ProcessLightList 接入渲染（2026-08-02）**——`GameState` 持有 `LightManager`（C++ `Lights[]`），`descend_to_level` 按 C++ `InitLevels` 依次 `init()` + `make_light_table(leveltype)` + `memcpy(dLight,dPreLight)` + `add_light(玩家, 半径)`（level_type 由层号 1:1 映射 L1-L4/Hellfire）；`GameState::update` 每 tick 先 `change_light_position` 让玩家灯跟随角色再 `process_light_list`（`game_logic` 的旧空桩已删），light_buffer 逐帧驱动；新增 `test_update_moves_player_light`（灯跟随玩家、中心 tile 全亮 0、邻 tile 按 falloff 衰减 <15）。**玩家光半径对齐 `_pLightRad`（2026-08-02，issue #7）**——`player_exact::Player._p_light_rad` 默认 10（C++ InitPlayer player.cpp:2338），`descend_to_level` 的 `add_light` 与 `render_world_pipeline` 玩家光晕（原硬编码 9）改用该值；`test_descend_to_level_enters_dungeon` 增断言（默认 10 + 灯半径跟随）。待办：装备 `_iPLLight` 加成（CalcPlrLightRadius 累加）依赖 InvBody 装备系统接线（`item_affix::calc_player_item_values` 为 TODO 死代码，未接 player_exact）。待办：城镇 NPC 入面、多光源实体灯（怪物/导弹/对象灯源）、**探索持久化（2026-08-02，issue #5）**——GameState 增 `explored` 网格，`update()` 地牢分支用 `cast_vision_rays`+`tile_allows_light`（与渲染同算法）每 tick 累积玩家视野 tile（C++ DoVision 的 dFlags::Explored）；`apply_light_occlusion` 增 `explored` 参数：已探索但当前不可见的 tile 保留 dLight（C++ 残留帧近似），未探索区仍全暗；`descend_to_level` 重置 explored；新增 2 测试（occlusion 保留已探索区 + descend 重置/update 累积） |
| 引擎基础 | mpq/audio/font/events/path/random/ticks | `engine/{mpq,audio,font,events,path,random,ticks}.rs` | RNG 序列与 C++ seed1 逐值一致；MPQ 真实解压测试（archive_manager 8 passed） | — |
| 游戏逻辑 |**Mushroom Quest 新状态（2026-08-02 审计，upstream 2c1a364da）**——`TalkToWitch`（towners.cpp:369-373）现要求 `HasInventoryItemWithId(player, IDI_BRAIN) && Quests[Q_MUSHROOM]._qvar2 != TEXT_MUSH11` 才重发 TEXT_MUSH11 并置 `_qvar2 = TEXT_MUSH11`（防止同一对话重复触发；`TEXT_MUSH11`=SpeechId::Mush11=131 已存在于 quest_new.rs）。Rust `dialogue.rs` 的 `WITCH_QUEST_MUSHROOM_BRAIN` 节点条件仅 `has_quest_item(p, 18)`（`DialogueOption::condition: fn(&Player)->bool` 无 quest 状态入参）且整套 dialogue 树为 `#[allow(dead_code)]` 静态节点（未接入城镇交互），`TalkToWitch` 状态机未移植——该 guard 在移植 Adria 对话状态机时一并落地，本项暂记待办；`Quest._qvar2` 字段已存在（quest_new.rs:407），数据结构无需变更。 根 `Source/*.cpp`（~63k 行） | `game/`（~109k 行，含测试与重叠实现） | player/monster/missiles/items/objects/stores/towners/quests/inv/loadsave/msg/control/effects/movie 等均移植；**QUESTDATA 重生成（2026-08-02）**——quest_new.rs `get_quest_data` 24 行中 Q_DIABLO 5 字段陈旧（qdlvl 16→15、qdmultlvl 16→-1、qlvlt Hell→None、bookOrder 14→23、qdmsg Doom1→TEXT_VILE3/Vile3）且 8 个 Hellfire 任务（Grave/Farmer/Girl/Trader/Defiler/Nakrul/Cornerstone/Jersey）未初始化（qdlvl 0 vs 17/9/17/19/17/21/21/9）；已按 questdat.tsv 全 9 字段重生成，SpeechId 枚举补 12 个缺失变体（BoneR/BloodY/BlindIng/BloodWar 235-238 + Grave7/Farmer1/Girl2/Defiler1/Nakrul1/Cornstn/Jersey4/Trader，值对齐 C++ textdat.h）；verify_tables.py 新增 quests 检查（24 行 0 差）；；**职业属性验证（2026-08-02）**——`get_class_attributes`/`get_player_combat_data` 与 `classes/<class>/attributes.tsv` 逐值一致（Warrior 30/10/20/25 max250/50/60/100 blk30 70/60/50；Rogue 20/15/30/20 max55/70/250/80 blk20 50/70/50；Sorcerer/Monk/Bard/Barbarian 亦全对；adjLife/adjMana 定点 2.6 解码同 C++ parseFixed6），新增 `test_class_attributes_match_tsv`；6 类 attributes.tsv 已入库 `tools/table_data/attrs_*.tsv`；**Towner 位置对齐 towners.tsv（2026-08-02）**——C++ `InitTowners`（towners.cpp:742）运行时从 `towners.tsv` 加载（townerdat.cpp:113-114 读 position_x/y），Rust `TOWNER_DATA` 6 个 NPC 位置原为旧硬编码数组值（DeadGuy 25,19→24,32、Cain 25,29→62,71、Farnham 55,71→71,84、Adria 80,62→80,20、Gillian 43,78→43,66、Wirt 75,68→11,53），已全部改为 TSV 权威值；新增 `test_towner_positions_match_tsv` 锁定全部 10 个位置；可玩 demo；timedemo 演示事件解码测试；**NetSendCmdString 对齐 + CmdId 重编号为 C++ `_cmd_id`**（386090e12/05fd5b7bc，**已再同步 upstream msg.h**：移除 CMD_ATTACKXY、新增 CMD_PLRALIVE，334bd9d75）；**DungeonSeeds 移植（2026-08-02，616b8d0 后续）**——GameState 新增 dungeon_seeds: [u32; 17]，按 C++ NetInit（multi.cpp:863-871）从 game seed 经 xoshiro128++ 链派生，dungeon_seeds[0]（城镇）用 GenerateSeed() 重随机以隔离商店；descend_to_level 改用 DungeonSeeds[currlevel] 生成布局（对应 C++ CreateLevel→CreateDungeon，diablo.cpp:1430），game_logic 与 place_dungeon_monsters 的 StdRng 用 DungeonSeeds[currlevel] 种子化（对应 SetRndSeedForDungeonLevel，diablo.cpp:3025-3034，原占位 seed_from_u64(0) 与 game_tick+0xC0FFEE 移除）；新增 `test_dungeon_seeds_derive_from_game_seed_via_xoshiro_chain`、`test_gameplay_rng_seed_matches_dungeon_seed`；**怪物 AI 随机游走去随机化（2026-08-02）**——`update_monster_movement` 空闲游走掷骰改用传入的层级种子 RNG（原 `rand::rng()` 非确定），`process_monsters_internal` 补接移动消费方，AI/移动测试全部改传确定性 `StdRng::seed_from_u64(1)` | Rust 自增命令（Chat/Pause/UseItem 等）在 81+，非原版线兼容（已注明）；重叠实现需清理；timedemo 全量回放仍 #[ignore] |
| 存档/加密 | loadsave/sha/pack/codec/encrypt | `game/{loadsave,sha,pack,codec}.rs`、`engine/encrypt.rs` | loadsave 目标为 C++ 二进制存档兼容（常量对齐） | 字节级互操作未用 C++ 存档实测 |

## 3. 部分对齐（⚠️）

| 子系统 | C++ 行数 | Rust 行数 | 差距 |
|---|---|---|---|
| `controls/` | 6,478 | 2,226 | 键盘移动/基础输入已移植；**keymapper/padmapper 模型已移植（2026-08-02）**——`controls/keymapper.rs` 移植 C++ `KeymapperOptions`/`PadmapperOptions`：53 个键盘动作 + 54 个手柄动作表逐项对照 `InitKeymapActions`/`InitPadmapActions`（diablo.cpp:1846-2478）含动态索引项（BeltItem/QuickSpell/QuickMessage 1..=N）、key 名称映射（`key_id_to_name` 对照 options.cpp:1098-1169）、`combo_description` 按 C++ `modifier+button` 格式（含 Shorten 3 字符短名）；新增 5 测试（动作表逐项默认值、key 名称表、combo 格式、按绑定查找）。注：keycode 存 SDL2 风格值（Rust 输入层现状），上游已迁 SDL3，桥接待输入层升级；触摸（touch/）、手柄（devices/joystick/game_controller）、运行时派发（keymapper.cpp/padmapper.cpp）未移植 |
| `utils/` | 9,079 | 3,174 | RNG/str_case/parse_int/paths/options/locale 等已移植（**autopickup 选项补齐**，675a832b0）；**已移除 upstream 删除的 adriaRefillsMana/FloatingNumbers 选项**（715d74d91）；utf8/str_split/surface_to_* /sdl_* 等未移植 |
| `panels/` | 2,263 | 3,562 | HUD 为自研实现，非逐行移植；DrawMain 未入调色板面 |
| `ui/`（对应 DiabloUI） | 5,720 | 4,835 | 自研替换实现（mainmenu/hero_select/dialogs/ui_core/ui_item）；**hero 默认名表/职业可用性/主菜单动作/枚举/credits 文本/HeroInfo 结构对齐**（58e4a34fc..b51510201）；**settings 菜单模型（2026-08-02）**——`ui/diabloui/settings.rs` 移植 `OptionCategoryBase`/`OptionEntryBase` 数据模型：类别按 C++ `GetCategories()` 相对顺序（Language/Graphics/Audio/Gameplay/Controller，键 `Game` 等逐项核对）、条目名/描述逐条取自 options.cpp（Run in Town、Enemy Health Bar、Auto Gold Pickup…共 35 项）、布尔条目 `change()` 直接翻转 `utils::options` 活选项、值描述反映当前状态；新增 4 测试（类别顺序与 C++ 键、Gameplay 条目名、布尔翻转活选项、Audio 值描述）。**settings 屏幕（2026-08-02）**——`SettingsMenu` 两级导航（Categories→Settings，Enter 下钻/循环值、Esc 回退/退出，C++ `ShownMenuType`/`GoBackOneMenuLevel`），`main.rs` 的 `show_settings_dialog` 由占位消息框改为真实 `ui_settings_dialog`（键盘 ↑/↓/Enter/Esc + 鼠标点击/悬停，行内显示 `名称: 当前值`），新增 `test_settings_menu_navigation`。**ListOption 子菜单（2026-08-02）**——`SettingsEntry` 增 `OptionEntryListBase` 值列表支持（`list_size`/`list_value`/`active_index`/`set_active_index`）；`SettingsMenu` 增第三级（C++ `ShownMenuType::ListOption`）：>2 值列表 Enter 打开子菜单（选中当前值），2 值列表直接循环（settingsmenu.cpp:292-296）；Audio 类别按 C++ 移植 Sample Rate `{22050,44100,48000}`、Channels `{1,2}`、Buffer Size `{1024,2048,5120}` 列表；Language 类别改为 en/zh_CN 列表；新增 `test_settings_menu_list_option_level`，Audio 值测试并入同一测试避免全局 options 竞争。**multi/ selconn（2026-08-02）**——`ui/diabloui/selconn.rs` 移植 `UiSelectProvider`（selconn.cpp）：连接类型列表（ZeroTier/Client-Server (TCP)/Offline，`net::storm::ConnType` + `connection_name`）、每项描述（SelconnFocus 文案逐字）与支持人数（Loopback=1、其余 MAX_PLRS=4）；`main.rs` 增 `ui_sel_conn_dialog`（键盘/鼠标，Enter 选定、Esc 取消）并接入 `init_multiplayer_menu`（先选连接再进英雄选择）；新增 `test_selconn_matches_cpp`（列表顺序/名称/描述/人数/回绕）。**multi/ selgame（2026-08-02）**——`ui/diabloui/selgame.rs` 移植 `UiSelGameDialog` 模型：游戏动作（Create Game/Create Public Game/Join Game，值 0/1/2 + `GameSelection_Focus` 描述）、难度列表（Normal/Nightmare/Hell，C++ `DIFF_*` 值 + `Diff_Focus` 描述）；`main.rs` `ui_sel_game_dialog`：loopback 直进难度表单（C++ 跳过动作列表），ZT/TCP 先选动作再选难度，返回 `SelGameForm`；接入 `init_multiplayer_menu`（selconn → selgame → 英雄选择）；新增 4 测试（难度名/值/顺序、动作值/描述、难度菜单导航、动作菜单）。**网络层 CreateGame/JoinGame（2026-08-02）**——`net::storm` 增进程级 SNet 状态（`snet_initialize`/`snet_create_game`/`snet_join_game`/`snet_leave_game`/`snet_is_game_host`，线程局部 slot）；`init_multiplayer_menu` 在 selgame 后实际初始化 provider（loopback）并 CreateGame（返回玩家 0）/JoinGame（loopback 不支持），`net_close` 离场；新增 `test_global_snet_create_game_loopback`。**KeyInput/PadInput 键位绑定（2026-08-02）**——`SettingsEntryType` 增 `Key`/`PadButton`（C++ `OptionEntryType::Key`/`PadButton`），`SettingsMenu` 增 KeyInput/PadInput 子菜单层（`in_binding_screen`/`binding_kind`/`binding_value`/`set_key_binding`/`set_pad_binding`，C++ `ShownMenuType::KeyInput`/`PadInput` + `selectedOption`）：Keymapping/Padmapping 类别按 `default_key_actions`/`default_pad_actions` 生成 53+54 条目，行显示 `动作名: 当前绑定`，Enter 进绑定屏（bound 值/捕获提示/Unbind 三行，C++ `UnbindKey`/`UnbindPadButton`），Esc 逐级回退；`main.rs` 增 `mod controls;`（bin 与 lib 共享 controls 模块树）；新增 7 测试（keymapper 5 + 绑定屏 2）。待办：SDL 键捕获接 UI 循环（当前 `set_key_binding` 由模型层接受 key-name）、pad 组合按键计时捕获、运行时派发 |待办：ListOption 子菜单（多值列表选择）、`multi/`（selconn/selgame）|待办：settings 屏幕（settingsmenu.cpp 多级 UI 状态机）、`multi/`（selconn/selgame）|

## 4. 未移植（❌）

| 子系统 | C++ 行数 | Rust 现状 |
|---|---|---|
| dvlnet + storm（多人网络） | 3,734 + 897 | `src/dvlnet/`（54 行）与 `src/storm/`（161 行）为空壳，未编译；`game/{multi,msg,nthread,pfile}` 仅本地框架；**已移植**：线上报文格式（net::packet）、回环传输（net::transport::Loopback）、会话层（net::base_protocol）、storm SNet* 接口（net::storm）、game::multi 接入（7f02549d2）、frame_queue 帧原语（442292b60）、TCP client/server 传输（net::tcp，6e405090f）、**TCP 加入握手 net::session**（06da9698c）；**msg 线上字节格式锁定（2026-08-02）**：`game/msg.rs` 新增 5 项测试——TCmd 结构体尺寸匹配 C++ `#pragma pack(1)`（msg.h:431，TCmd=1/TCmdLoc=3/TCmdParam1=3/TCmdParam2=5/TCmdLocParam1=5/TCmdLocParam4=11），`send_walk`→[1,x,y]、`send_attack_id`→[15,id_le16]、`send_spell_xy`→[12,x,y,4×i16 LE] 逐字节对齐 C++ SendWalk/SendAttackId/SendSpellXY，`parse_command` 往返；**storm `conn_type`/`ConnectionNames`（DiabloUI multi selconn 数据）对齐**——`ConnType` 枚举（ZT=0/TCP=1/LOOPBACK=2，storm_net.hpp:14-19）+ `connection_name`（selconn.cpp:29-32：ZeroTier/Client-Server (TCP)/Offline），新增 `test_conn_type_and_names_match_cpp`；**net::packet PT_JOIN_REQUEST 字节格式锁定（2026-08-02）**——C++ `packet_proc::process_data`（dvlnet/packet.h:240-242）`[type][src][dest][cookie LE u32][info]`，新增 `test_join_request_bytes_match_cpp_layout`；**其余报文类型字节锁定（2026-08-02，616b8d0）**——PT_JOIN_ACCEPT `[cookie LE u32][newplr u8][info]`、PT_CONNECT `[newplr u8][info]`、PT_DISCONNECT `[newplr u8][leaveinfo LE u32]`、PT_ECHO_REQUEST/REPLY `[time LE u32]`、PT_INFO_REQUEST/REPLY `[info]`，新增 `test_join_accept/connect/disconnect/echo/info_packets_bytes_match_cpp_layout`；**门命令字节格式（2026-08-02）**——`send_open_door`/`send_close_door`（TCmdLoc `[39/40][x][y]`）+ 3 测试；**命令分派（2026-08-02）**——`GameState::handle_command` 接 msg 层 → `objects::sync_op_object`（门命令端到端，含测试） |
| lua（脚本系统） | 2,905 | `src/lua/` 32 行空壳，未编译；**Lua 引擎核心 + 全部 9 个通用模块 + debug dev 模块已移植**（log/i18n/hellfire/audio/render/monsters/player/towners/items + devilutionx.dev，game::lua，3a189e337..441289699）；dev.search 全功能（接 game::debug automap 高亮）、display.grid/path/scrollView/vision 与 player.god/invisible/trn 开关已接 debug 状态；**全局引擎接入二进制（2026-08-02）**——`main.rs LuaInitialize/LuaShutdown` 实装：创建 LuaEngine + 注册 `devilutionx.*` 模块，`game_logic` 每 tick 调 `sync_player` 把真实玩家名/等级推入 Lua registry（`devilutionx.player.self()` 现返回实时角色），`call_event` 供游戏循环派发；线程局部全局（mlua::Lua 非 Sync）；新增 2 测试（sync_player registry 脚本读取 + 无引擎安全 no-op）。**towners.position() 实时化（2026-08-02）**——`sync_towners` 把 `GameState.towners` 活列表推入 Lua registry，`devilutionx.towners.<shortName>.position()` 返回真实城镇坐标（缺省 NPC → nil，对齐 C++ `GetTowner` null）；短名表 `TOWNER_SHORT_NAMES` 与 C++ `TownerShortNames` 逐项一致（griswold..nut）；game_loop 城镇分支每 tick 同步；新增 `test_towners_position_returns_live_positions`。剩余：walk_to/items 实时化、mod 脚本自动加载 |
| platform（各平台接入） | 1,602 | `src/platform/` 325 行空壳，未编译；**locale 转换已移植**（utils::locale::ietf_to_posix，9fb554f69）；其余多为主机专属（3DS/Switch/Vita/Android），桌面版不适配 |
| qol（便利功能） | 1,723 | `src/qol/` 68 行空壳，未编译；**全部 7 个 QoL 模块核心已移植**（含测试）；autopickup 已接活 GameplayOptions（896aa2804）并补 **AutoPickup town gate**（C++ `AutoPickup`：城镇且 `autoPickupInTown` 关闭则跳过，新增 `test_town_gate_matches_cpp`）；**FloatingNumbers 渲染接线（2026-08-02）**——`FloatingNumbers` 队列移植 C++ `FloatingQueue`（同 id 100ms 合并窗口 `FLOATING_MERGE_TICKS`、2500ms 寿命 `FLOATING_LIFETIME_TICKS`、`clear_expired`/`age` 上升曲线）；`GameState` 持有队列并在每 tick 清理过期项，玩家命中/击杀（Hit/Kill）在怪物 tile 上按 `FloatingNumber::new`（物理伤害、monster_id 作合并键）入队；`draw_floating_numbers` 按 C++ `DrawFloatingNumbers` 投影/上升/颜色（Gold/Silver/Blue/Orange）+ 字号（12/24/30→PixelFont 1/2/3）；新增 2 测试（合并/过期、age 曲线）。**ItemLabels 渲染接线（2026-08-02）**——`ItemLabel` + `layout_non_overlapping` 移植 C++ `DrawItemNameLabels` 行带防重叠（`UsedX` + `BorderX=4` 水平间距）；`draw_item_labels` 按 `showItemLabels` 选项（C++ `IsHighlightingLabelsEnabled`）为地面物品绘制名称标签（金币 `"{value} gold"`、真实物品 `item_label_text`、药水 `display_name`），标签位于 tile 上方；新增 3 测试（同行不重叠保留/重叠跳过/不同行不冲突）。**MonHealthBar 渲染接线（2026-08-02）**——`find_hovered_monster`（C++ `pcursmonst` 悬停查找：鼠标→tile→首个存活怪物）；`draw_monster_health_bar` 按 `enemyHealthBar` 选项在地牢中把悬停怪物血条画在屏幕顶部（`bar_metrics` 几何、lifesteal 蓝色填充（C++ healthBlue）、边框/背景），CLX 美术用矩形替代；新增 `test_find_hovered_monster_by_tile`（命中/跳过死亡/空格）。剩余：chatlog、stash、xpbar 渲染接线 |
| discord | 193 | `src/discord/` 14 行空壳 |
| portals | 50 | `src/portals/` 15 行空壳 |
| levels/crypt（Crypt 关卡） | 840 | 无对应模块 |

## 5. 已移植代码中的已知行为偏差（待修复，按优先级）

1. **光照/视野（深度核实 2026-08-01）**：C++ 快照**无** `MakeLight`——真实机制为径向 `DoLighting` + 射线 `DoVision` + `dPreLight` + 多光源 `ProcessLightList`。已移植 `cast_vision_rays`（VisionRays 表 + 四象限 + 对角线遮挡）；`Dungeon.light/pre_light` 默认值已对齐 C++ `defaultLight`（0 town/15 地牢，7bf225347）。可玩关卡（L1/town）渲染光照基底**已与 C++ 一致**；`DoVision` 属 automap/探索范畴（C++ 渲染不以 dFlags::Visible 门控）。**静态光烘焙（2026-08-02 核实）**：`apply_static_lights` 对 Caves 熔岩（dPiece 55-146/153-160/149/151）按 C++ `PlaceCaveLights`（drlg_l3.cpp:2150-2163，半径 7）逐条对齐；`light_falloff`/`light_cone_distance` 与 C++ `MakeLightTable` 公式逐值一致（新增回归测试 `test_light_math_matches_cpp_make_light_table`：radius 8 的 0/8/36/72/73 距离与 cone 3,4→40 / 5,12→104）；offset={} 时 Rust 方形扫描等价于 C++ 四象限 `RotateRadius` 走查。**视觉射线墙遮挡修复（2026-08-02）**：`render_world_pipeline` 的 `cast_vision_rays` 此前 `tile_allows_light` 硬编码 `|_| true`（射线穿墙 → 墙后 trans 区域被误标透明）；现按 C++ `TileAllowsLight`（lighting.cpp:93，SOL `BlockLight`）实现 `lighting::tile_allows_light` 并接入，新增 `test_tile_allows_light_matches_cpp` 与 `test_cast_vision_rays_stop_behind_block_light_wall`。**L1 大教堂熔岩静态光烘焙（2026-08-02）**：`apply_static_lights` 增补 Cathedral 分支——dPiece 269（熔岩）→ `OBJ_L1LIGHT`（C++ AddL1Objs objects.cpp:3756 + AddObjectLight 半径 5）→ dPreLight；新增 `test_cathedral_layout_bakes_lava_pre_light`（合成布局 269 瓦片被点亮、远处保持全暗 15）。L1 的 269 瓦片仅出现在完整版 L3-L4 大教堂（spawn 数据无熔岩，无法用 spawn MPQ 实证，逻辑由合成测试覆盖）。**L2 火炬静态光已移植（2026-08-02）**——`add_l2_torch_lights` 按 C++ `AddL2Torches`（objects.cpp:452-478）+ `AddObjectLight`（radius 8）逐格扫描 dPiece（0/4/36/40 触发 FlipCoin 3/10，36→NW(-1,0)、40→NE(0,-1)，`IsObjectAtPosition` 用本 pass 已放置集合近似），RNG 流模拟 `InitObjects`（`SetRndSeedForDungeonLevel` 种子 + `DiscardRandomValues(1)（objects.cpp:3823）），接入 `generate_dungeon_layout` L2 分支烘焙进 dPreLight；新增 `test_l2_torch_lights_place_and_lit`（seed 0 四掷 F/T/F/T → (20,10) 与 (40,9) 点亮、失败掷保持全暗）与 `test_l2_grid_without_triggers_stays_dark`。注：Rust 简化层号用 `dungeon_seeds[2]`（C++ L2 为 seeds[5..8]），quest 门控前置 RNG 消耗（L2-4 storybook）未复刻；L4 无火炬对象（AddL4Goodies 为尸体/刑具/坩埚，无光）。
2. **透明度（已实现主体 2026-08-01；L1 补齐 2026-08-02）**：`FloodTransparencyValues`（span-fill，gendung.cpp:820/286）已移植并接入 L1 布局（00a53e5ef）；`scrollrt` 已按 C++:540 接入 `TransList[dTransVal[tile]]`，`draw_cell` 使用 getFirstTileMaskLeft/Right + 墙体 Transparent mask，`render_world_pipeline` 按 DoVision markTransparentFn 填充 TransList（1737a44f0）。**L1 `FixTransparency`（drlg_l1.cpp:1031-1064，Dirt 墙 2x2 足迹传播）与 `EntranceStairs` trans 拷贝（drlg_l1.cpp:1202-1211）已移植并接入 `build_dungeon_layout`**：drlg_l1.rs 新增 `fix_transparency`/`copy_stairs_transparency` + 单元测试；新增 C++ 交叉验证工具 `tools/l1trans_repro/`（逐字拷贝 gendung.cpp/drlg_l1.cpp 透明度函数），seeds 1/2588/743271966/999/424242 的 112x112 dTransVal 与 Rust 0/12544 偏差。**门/窗对象放置已移植（2026-08-02）**——`scan_level_doors` 按 C++ `AddL1Objs`/`AddL2Objs`/`AddL3Objs`（objects.cpp:3756-3795）纯 dPiece 扫描（无 RNG）：L1 43/50/213→L1LDOOR、45/55→L1RDOOR；L2 12/540→L2LDOOR、16/541→L2RDOOR；L3 530→L3LDOOR、533→L3RDOOR（L4 无门对象），已接入 `descend_to_level` 填充 `game_state.objects`（怪物开/关检测 monster_check_doors 与位置阻挡 is_position_blocked_by_object 可见真实门）；新增 `test_scan_level_doors_matches_cpp_micro_values` 锁定映射。**门开关交互已移植（2026-08-02）**——`GameState::operate_door` 按 C++ `OperateDoor`（objects.cpp:1762-1785）+ `IsDoorClear`（1553-1559，检查玩家/怪物/地面物品占位）+ `SetDoorStateOpen/Closed`（1055-1160）：开关切换 door_state/ovar4（DOOR_CLOSED=0/OPEN=1/BLOCKED=2）并改 dPiece 微值（L1 开 392/394 闭=原位；L2 开 12/16 闭 537/539；L3 开 537/540 闭 530/533）；`init_doors_closed` 按 `AddDoor`（1178-1198）在放置时烘焙闭态微值；点击处理接入（点门 = 开关门，C++ `CheckObject`→`OperateDoor` objects.cpp:4314）；新增 `test_operate_door_toggles_micros`（540→537→12→537 逐值）与 `test_operate_door_blocked_refuses_close`（怪物占位 → DOOR_BLOCKED 保持开态）。**门网络命令发送层（2026-08-02）**——`MsgHandler::send_open_door/send_close_door`（`NetSendCmdLoc(CMD_OPENDOOR/CLOSEDOOR, pos)`，body=TCmdLoc `[39/40][x][y]`，CmdId 与 upstream msg.h 逐值一致），新增 3 测试（开/关字节格式 + 收发往返）；接收侧 `objects::sync_op_object` 的 `SyncCmd::OpenDoor/CloseDoor` 早已存在。**命令分派器（2026-08-02）**——`GameState::handle_command`（C++ `run_cmd`/`OnOperateObject`）：CMD_OPENDOOR/CLOSEDOOR 的 TCmdLoc 按坐标定位门对象并经 `objects::sync_op_object`（远程语义 is_local_player=false）应用，新增 `test_handle_door_commands_from_network`（msg 字节 → 开门/关门/无门 no-op/畸形数据 no-op）；顺带修复 `Object::set_door_state` 与怪物开门路径不同步 `door_state`/`ovar4`（C++ `_oVar4` 镜像）。待办：其余命令分派（walk/attack/spell…）与门精灵渲染（开态透明度）。
3. **foliage（已完成 2026-08-01）**：`RenderTileFoliage`/`GetDunFrameFoliage`（dun_render.hpp:132/162）已移植（776defcf0），`draw_cell` 地板 TransparentSquare tile 渲染 16 行 foliage 层（y-16..y-31）。
4. **实体/HUD 入面**：实体 overlay 已统一到 `tile_screen_position`（64f98e886），但未绘入调色板面；HUD/DrawMain 未入面。
5. **多关卡衔接（2026-08-02 已端到端验证）**：`check_stairs_transition` 已支持 4 层下降（L<4 NextLevel）、任意层上升（L>1 PrevLevel→上一级、L1→town）与 town warp；`descend_to_level` 逐层生成布局、重定心、装楼梯触发、放置怪物。**新增链式全流程回归测试 `test_full_level_flow_town_l1_l2_l3_l4_and_back`**：town→L1→L2→L3→L4→L3→L2→L1→town 逐级踩楼梯验证（合成 art，无 MPQ 依赖）；`l2_stairs_flow` 用真实 MPQ 验证 L2 楼梯触发 dPiece（266/270/558）。L1 up-stair 检测仅对 Cathedral（TIL mega）；**L2-L4 楼层间 up-stair 已定位（2026-08-02，issue #10）**——`find_level_up_stairs` 按 C++ `InitL*Triggers` PrevLevel 扫描（trigs.cpp）：L2/L3/L4 的 dPiece 楼梯 tile 266/170/82，`descend_to_level` 对所有层填充 `dungeon_up_stairs`；新增 `test_find_level_up_stairs_detects_cpp_tiles`（四层 tile 检测 + 无 tile/不支持层返回 None），链式测试补 L1 up-stair 断言 由 `init_dungeon_triggers` 的 dPiece 扫描安装。
6. **存档互操作（2026-08-02 字节级验证）**：**vanilla codec 实装**——`game/codec.rs` 原为占位桩（无真实 SHA1/XOR），已按 `Source/sha.cpp`（X-SHA1，算术右移变体）+ `Source/codec.cpp`（CodecInitKey 用**独立** context 计算 key 哈希——原 Rust 错误复用 pw 上下文、由 C++ 交叉验证发现）完整移植：`codec_encode`/`codec_decode` 逐块 XOR + 校验和签名。**真实 C++ 存档解码通过**：`tests/timedemo_e2e.rs::decodes_real_cpp_save_game_entry` 用 `spawn_0.sv`/`demo_0_reference_spawn_0.sv`（spawn 单人密码 `adslhfb1`）解码 `game` 条目 → magic `SHAR`、setlevel=0、currlevel=1（WarriorLevel1to2 开局存档），`hero` 条目 → 1266 字节英雄 blob；错误密码被校验和拒绝。X-SHA1 原语/CodecInitKey/64B 全管线编码与独立 C++ 编译的 sha.cpp+codec.cpp 输出逐字节一致。**Tier 1.5 头解析（2026-08-02）**：新增 `loadsave::CppGameHeader`（C++ `SaveGameData` 头，magic LE u32 + setlevel LE u8 + BE u32 系列 + 计数），`decodes_real_cpp_save_game_entry` 对真实 `spawn_0.sv`/`demo_0_reference_spawn_0.sv` 解码后完整解析并断言 magic=SHAR、setlevel=0、currlevel=1、leveltype=1（Cathedral）、计数非负；**level-seed 表解析**（loadsave.cpp:2803-2806，17 层 × BE u32 seed + BE u32 leveltype）断言 level0=Town(0)、level1=Cathedral(1)、当前层 seed 非零。**Tier 2 英雄 PlayerPack 解析（2026-08-02）**：`decodes_real_cpp_save_game_entry` 对真实 `spawn_0.sv` 解码 `hero` 条目（pfile.cpp EncodeHero = `#pragma pack(1)` `PlayerPack` 加密体）并用 Rust `pack::PlayerPack::from_bytes` 解析——class=0（Warrior）、name=`timedemo`、plrlevel=1，与 WarriorLevel1to2 开局存档一致。待办：Tier 3 将 inner 结构完整喂入引擎 save loader 做往返字节比对（timedemo replay）。**pfile 层同步接入 vanilla codec**（`game/pfile.rs` 原为占位 identity codec + `len+16`）：改为调用 `game::codec`，`IsHeaderValid` 按 C++ `loadsave.cpp:2304`（SHAR/SHLF 恒有效；RETL/HELF 仅非 spawn）实装，新增 `test_is_header_valid_matches_cpp` 与 pfile codec 往返测试。
6. **关卡生成 fidelity（实证 2026-08-01）**：Rust 各层生成器对 C++ gold `.dun` 导出的逐格匹配率：**L1 100%** / **L2 100%（非 quest，fixture 5-1677631846；2026-08-02 对齐）** / **L3 100%（fixture 9-262005438 + 10-879635115 Q_ANVIL quest 房，2026-08-02 对齐）**/ **L4 100%（2026-08-02 完全对齐，13-16 层全部 fixture 与独立 C++ 复现逐格 1600/1600）**：FixTilesPatterns 按 C++ pass 内原始顺序整函数重生成（157 条规则 + `!=0` 规则 + 边界守卫 `<40`/`>=0`）、PlaceMiniSet `tries=199`（原误用 1600）、L15 闭门五芒星 L4PENTA 覆盖 L4PENTA2（记录 hell gate 位置后直写）、L16 Diablo 巢穴（load_dun_file 修复：路径回退 `test/fixtures/levels/l4data/`、去严格尺寸检查、tile 0→floorId=6、放置 tile 置 Protected）、**Warlord quest 房（13-594689775）**：warlord_quest_active 标志 → first_room 固定 11x11 + SetPieceRoom、init_set_piece 放 warlord.dun、SetPieceRoom 保护、PlaceStairs 跳过 down-stairs；13-428074402 / 13-594689775(warlord) / 14-717625719 / 14-815743776 / 15-1256511996 / 15-1583642716 / 16-741281013 全部 0 偏差。⚠️ fixture 13-594689775 / 15-1256511996 与 current HEAD C++ 有 11/1063 格差异——fixture 用旧资产（warlord.dun 右墙缺失版 / 2022 旧导出），C++ 复现程序同样 mismatch，属 fixture 过期而非 Rust 偏差——生成器为近似重实现，非字节精确；`tests/dun_fixture_alignment.rs` 为持续对齐回归基线，每层提升应在此提高匹配率。**L2 非 quest 完全对齐（2026-08-02，5-1677631846 逐格 1600/1600）**：修复 define_room 角标（C++ TL='C'/BL='E'/TR='B'/BR='A'，原 Rust 错位且墙循环覆盖角标 → ConnectHall 无法转向）、fix_lockout pass2/3 用循环游标推进（C++ 扫完走廊后外层跳过整段，Rust 独立游标导致后半段误放门）、PlaceMiniSetRandom 改为 C++ 全网格扫描 + 每候选 GenerateRnd(100)（原为 predungeon 随机尝试 + 单次概率）、VARCH 顺序对齐（C++ VARCH33-40 先于 VARCH1-32）、主题房（DRLG_PlaceThemeRooms/GetSizeForThemeRoom/CreateThemeRoom/IsNearThemeRoom）移植、HARCH1（尺寸 3x2 内容错）与 PANCREAS2（109→110）数据修正、MAX_ROOMS 80（C++ `nRoomCnt >= 80`）。**L2 quest 路径已完全对齐 current HEAD C++（2026-08-02，独立复现程序逐格 1600/1600）**：Q_BLOOD（L5）修复 `place_miniset` stairs 扫描 setpiece 分支 `continue` 跳过 `x += 1` 与 `Substitution` SetPieceRoom 跳过；**Q_SCHAMB（L6）/ Q_BLIND（L7）**——quest 标志门控 quest 房（原无条件创建）、bonestr2.dun（7x7）/ blind1.dun（11x11）set piece 嵌入、init_set_piece 按 level 分派并防边缘越界；5/6/7/8 层与 C++ 复现逐格 1600/1600。⚠️ **5-68685319 / 6-* / 7-* fixture 已过期**：其 quest 房为普通房间、无 blood1 内容（55-63/156-157 全缺席），而 current HEAD C++（独立复现程序验证）会放置 blood1（与 gold 差 49 格全为 blood1 tile）；同 L1-2588/L3-262005438 列，需 C++ 重导出。**新增 C++ L2 独立复现程序 `tools/l2repro/`**（g++ 编译 drlg_l2.cpp + 拷贝的 gendung 辅助函数 + stub 头；`l2repro.exe <seed> <quest> [pd|th]`）——验证 current HEAD C++ 输出、RNG 检查点与 predungeon dump，作为 L2/L3 真值基线。L3 循环结构（PlacePool 作 break + minisets 移至后循环）、Warp/HallOfHeroes、尾部（PlaceMiniSet 199 tries / SpawnEdge / River 全量 / ThemeRooms(caves) / Fence / CanReplaceTile）均已移植；**fidelity 27.0%→83.3%**。剩余 267 格差异主要来自过期 fixture（2022-06 旧 drlg_l3.cpp，PlaceMiniSet 为 BYTE 签名），需用当前 HEAD C++ 重导出 fixture 才能验证 L3 是否 100%。
7. **L1 生成 fidelity（已完成 2026-08-01）**：Tile 枚举已全面重对齐 C++ `drlg_l1.cpp`（全部 L1 值：Corner=3/DWall=4/…/DirtHwall=18/Dirt=22/EntranceStairs=64/Floor22=162/…），minisets 用 C++ TIL mega 直写值（57–68 楼梯、128–130 灯、202/200/84/199/203/83/85/206/80/81/134/135 毒水池），完整移植 AddWall（Corner/VWallEnd/HWallEnd/HWall/VWall 六分支 + arch/fence 变体 + DoPatternCheck 后最终匹配）、PlaceMiniSet（随机起点+回绕扫描+drlg1Quirk x/y>12 偏置）、PlaceStairs（含 pOriginalCathedral → **L5STAIRSUP**，fixture 用原始大教堂楼梯，此为最后 8.1% 缺口根因）、GenerateLevel 重试循环（minarea=533 + 楼梯成功才 break）、FixDirtTiles/FixCornerTiles/Substitution/ApplyShadowsPatterns（BaseTypes/TileDecorations/ShadowPatterns 207/37 表从 C++ 源程序化抽取）。**fidelity 28.2% → 100%（1600/1600，seed 743271966）**。独立 C++ 复现程序（l1repro）与 Rust 逐格一致（0 偏差）。⚠️ 另一 fixture 1-2588 仍 447/1600：该 fixture 由 2022-06 旧代码导出（之后 FirstRoom/FillChambers 等多次行为变更），C++ 复现程序同样 mismatch 1153——fixture 已过期，非 Rust 偏差。
8. **Timedemo 全量回放**：`replay_warrior_level1to2`（C++ gold 字节比对）仍 `#[ignore]`；**Tier 1 无头驱动已落地（2026-08-02）**——`ReplayDriver::game_tick_count`（实测 WarriorLevel1to2 = 4,853 GameTick）+ 新增 `headless_replay_drives_game_ticks`：把 demo 的 GameTick 事件流跑进真实 `GameState::update`（无 SDL），两次运行状态完全一致（确定性）；输入→动作映射为 Tier 2。**Tier 2 点击移动已落地（2026-08-02）**——demo 主输入为鼠标点击（329 次，点击寻路；按键仅 UI 开关：Tab/i/c/1/Esc），headless_replay_applies_click_to_move 把 MouseButtonDown → convert_screen_to_tile → move_target，GameTick → 	ick_move_target + GameState::update，200 tick 内玩家从出生点位移且两次运行一致；	ick_move_target/convert_screen_to_tile 转 pub 供无头驱动复用。Tier 3 = 字节比对。**全量回放管线（2026-08-02）**——`full_replay_runs_to_completion` 把整个 WarriorLevel1to2 demo 流（**4,853 GameTick**，与 C++ `RunTimedemo` 实测一致）跑进真实 `GameState::update` + click-to-move（每轮重置 gameplay LCG，两次运行逐值一致），玩家从出生点位移显著（最终 (104,4)）；字节比对剩 save writer 与 C++ 逐字节对齐。**RNG 统一起步（2026-08-02）**——新增线程局部 gameplay LCG（`engine::random::{seed_gameplay_rng, gameplay_rnd}`，C++ `GenerateRnd(max-min+1)+min` 逐值一致，2 测试证明与 DiabloGenerator 同种子逐值相同 + 种子可复现）；`descend_to_level` 按 C++ `SetRndSeedForDungeonLevel` 每层种子一次；`game_state` 5 处掷骰（掉落判定×2、药水回退、怪物游荡×2）迁移到 LCG（每 tick 连续推进，对齐 C++ 而非每 tick 重置）。**切片 2（2026-08-02）**——objects.rs 神殿效果 6 处随机属性掷骰（Mysterious/Weird×3/Sparkling/Solar，C++ `GenerateRnd(4)`）与 effects.rs 音效变种 2 处（`GenerateRnd(2/3)`）迁移到 `gameplay_rnd`（原用线程 `rand::rng()`，现 LCG 确定性）。**切片 3（2026-08-02）**——combat_integration.rs 全部 6 处战斗掷骰迁移到 `gameplay_rnd`：怪物→玩家命中/格挡/伤害（`GenerateRnd(100)`×2 + `RandomIntBetween(min<<6,max<<6)`）与玩家→怪物命中/伤害/暴击（`GenerateRnd(100)`×2 + `RandomIntBetween(min,max)`）；两函数 `rng` 参数改 `_rng`（保留签名）；11 个 combat 测试在默认种子 0 下全绿（命中掷=0 恒命中，对齐各测试期望）。**切片 4（2026-08-02）**——player.rs 全部 12 处迁移：伤害区间 `RandomIntBetween`、命中/格挡/暴击/偷取概率 `GenerateRnd(100/201)`、偷取量、基础属性掷 `GenerateRnd(10)+1` 等；移除 4 个局部线程 RNG 绑定 + 未用 `use rand::Rng`。**切片 5（2026-08-02）**——missiles.rs 全部 10 处（`rand::rng()` → `gameplay_rnd`）：飞行时间、伤害区间（`RandomIntBetween`）、法术伤害 `GenerateRnd(10)`、动画帧、法力基值、散播偏移 `GenerateRnd(5)-2`、治疗/伤害、陨石伤害、随机法术选择；95 个 missile 测试全绿。**切片 6（2026-08-02，最大块）**——monster.rs 全部 67 处迁移：AI 决策掷（`GenerateRnd(100)`×35）、ai_delay 随机（`GenerateRnd(10/15/20)`）、移动/方向（`GenerateRnd(8/2/4)`）、位置/类型选择（`GenerateRnd(len)`）、怪物伤害 `RandomIntBetween(min,max)`、左/右偏好 `GenerateRnd(2)`；逐点补齐类型转换（roll i8/u8/usize、goal_var2 i8、dir_idx usize）；移除 37 个局部线程 RNG 绑定；58 个 monster 测试全绿。累计迁移 **~117 处**，**游戏玩法 RNG 迁移完成（2026-08-02）**——切片 7：item_affix 4（SelectAffix 权重掷、staff 法术选择、GetItemIndexForDroppableItem 加权、RndAllItems 金掷）、quest_new 1（任务池随机停用）、game_loop 2（地牢怪物放置 tile/类型）、Monster::new_with_rng 参数置 _rng；全库非测试 random_range 仅剩 items.rs 29 处遗留实现（已被 item_affix.rs 取代，后者走物品 RNG 与 C++ SetRndSeed 一致，故意不迁 gameplay）。

## 6. 测试覆盖（当前）

| 目标 | 结果 |
|---|---|
| `cargo test --lib` | 2627 passed / 2 ignored |
| `cargo test --bins` | 2566 passed / 2 ignored |
| `cargo test --test animation_validation` | 1 passed |
| `cargo test --test archive_manager` | 8 passed |
| `cargo test --test timedemo_e2e` | 5 passed / 1 ignored |
| `cargo test --test l2_stairs_flow` | 1 passed（shareware 下自跳过，实为待完整数据） |
| `cargo test --test dun_fixture_alignment` | 1 passed — 逐格 fidelity 基线：**L1 100% / L2 100% / L3 100% / L4 100%**（L1：本轮完成完整对齐——Tile 枚举重对齐 + C++ minisets（STAIRSUP/STAIRSDOWN/LAMPS/PWATERIN/L5STAIRSUP）+ AddWall/PlaceMiniSet/PlaceStairs/重试循环 + FixDirtTiles/FixCornerTiles/Substitution/ApplyShadowsPatterns；fixture 1-743271966 逐格 1600/1600；最后缺口为 `pOriginalCathedral` 触发 L5STAIRSUP（fixture 用原始大教堂楼梯），修复后 91.9%→100%。fixture 1-2588 为 2022-06 旧代码导出，过期不可复现。）（L4 已补 `memset(dungeon, 30)` 岩石背景，14.8%→20.9%）（L2：2026-08-02 完成非 quest + quest 全路径对齐——define_room 角标、fix_lockout 游标、PlaceMiniSetRandom 网格扫描、VARCH 顺序、主题房、HARCH1/PANCREAS2 数据、MAX_ROOMS=80、InitSetPiece、stairs 扫描 x+=1 修复、Substitution SetPieceRoom 跳过；5-1677631846 fixture 逐格 1600/1600，5-68685319 与独立 C++ 复现逐格 1600/1600。⚠️ 5-68685319 fixture 本身已过期——quest 房无 blood1 内容，需 C++ 重导出）（L3：AddFenceDoors 重写、Fence 边界、Q_ANVIL quest 房（FloorArea + PlaceAnvil + 河阻 hack）、PlaceLavaPool 边界 `dunx-1>0`；9/10/11/12 fixture 全 1600）（L2：6/7 层 Q_SCHAMB/Q_BLIND quest 房 + set piece 移植，与 C++ 复现 1600/1600；6/7 fixture 过期）（**fixture 回归测试已扩展覆盖 1-16 全部层 + quest 变体**；剩余缺口：L1 2-4 层 quest 房 237-382——其余 L2/L3/L4 全部对齐 current HEAD C++：L4 13-16 层 Warlord quest 房、魔印、Diablo lair 与独立 C++ 复现逐格 1600/1600；13-594689775/15-1256511996 fixture 因旧资产/旧导出与 current C++ 有 11/1063 格差，非 Rust 偏差） |
| `cargo test --doc` | 19 passed / 7 ignored |

## 7. 更新方法

每完成一个子系统/修复一个偏差：

1. 更新本矩阵对应行的状态与证据（附 C++ 行号引用或测试名）。
2. 运行 `cargo test`（全目标），确认 0 失败。
3. 提交（Conventional Commits），如 `feat: port MakeLight flood-fill` / `fix: ...`。
4. 完成“未移植”行后，将对应空壳目录从 `src/` 移除或接入模块图，并更新第 1 节总览。

## 8. 持续对齐回归（CI，2026-08-02）

新增 `.github/workflows/rust-alignment.yml`：push/PR 到 `devilutionx-rs` 分支时——
1. `cargo-test` job：全量 `cargo test`（含 fixture 生成 fidelity、真实 C++ 存档 codec 互操作、town↔L1..L4 楼梯 E2E；MPQ 缺席时相关测试自跳过，fixture 均入库）+ 打印 `dun_fixture_alignment` 逐层匹配率；
2. `cpp-repros` job：g++ 编译 `tools/l4repro`（current Source/levels/drlg_l4.cpp，无 SDL 依赖）并跑一个 L4 seed 冒烟，编译 `tools/l1trans_repro`（透明度交叉验证）。
