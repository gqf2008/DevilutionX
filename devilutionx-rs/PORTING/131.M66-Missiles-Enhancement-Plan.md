# M66: Missiles Enhancement - 完整实现计划

**日期**: 2025-12-07
**状态**: 📋 计划中
**预计时间**: 3-4天
**预计新增代码**: ~2,000行
**当前基础**: missiles.rs (4206行)

---

## 📊 现状分析

### 已完成（基础框架）
- ✅ MissilePosition 结构体
- ✅ MissileSource/MissileCaster 枚举
- ✅ 基础Missile结构体
- ✅ MissileManager框架
- ✅ 部分导弹类型处理

### 需要完善（M66目标）
- ❌ CheckMissileCol - 完整碰撞检测系统
- ❌ GetMissileVel - 导弹速度计算
- ❌ 伤害计算与抗性系统集成
- ❌ 特殊导弹效果（爆炸、连锁、area-of-effect）
- ❌ 导弹与怪物/玩家/对象的交互
- ❌ 完整的导弹更新循环

---

## 🎯 实现计划

### Day 1: 碰撞检测系统 (CheckMissileCol)

**目标**: 实现完整的导弹碰撞检测逻辑
**C++ 参考**: `Source/missiles.cpp:485-555`
**预计代码**: ~500行

#### 实现内容

1. **CheckMissileCol 函数** (~200行)
   ```rust
   pub fn check_missile_col(
       missile: &mut Missile,
       damage_type: DamageType,
       min_damage: i32,
       max_damage: i32,
       is_damage_shifted: bool,
       position: Point,
       dont_delete_on_collision: bool,
       only_hit_walking: Option<Direction>,
   ) -> CollisionResult
   ```

   - 检测与怪物的碰撞（MonsterMHit调用）
   - 检测与玩家的碰撞（Plr2PlrMHit调用）
   - 检测与对象的碰撞（ObjectMHit调用）
   - 处理碰撞后的导弹状态

2. **MoveMissileAndCheckMissileCol** (~100行)
   ```rust
   pub fn move_missile_and_check_missile_col(
       missile: &mut Missile,
       damage_type: DamageType,
       min_dam: i32,
       max_dam: i32,
       ignore_start: bool,
       if_collides_dont_move_to_hit_tile: bool,
   )
   ```

3. **碰撞结果类型** (~50行)
   ```rust
   pub enum CollisionResult {
       None,
       Monster { id: usize, damage_dealt: i32 },
       Player { id: usize, damage_dealt: i32 },
       Object { id: usize },
       Wall,
   }
   ```

4. **辅助函数** (~100行)
   - `is_missile_at_tile()` - 检查导弹是否在指定位置
   - `get_collision_targets()` - 获取碰撞目标列表
   - `apply_collision_damage()` - 应用碰撞伤害

5. **测试** (~50行)
   - 基础碰撞检测测试
   - 边界情况测试
   - 多目标碰撞测试

**成功标准**:
- ✅ CheckMissileCol 完整实现
- ✅ 10个测试全部通过
- ✅ 与combat系统集成

---

### Day 2: 导弹速度与伤害计算

**目标**: 实现导弹速度计算和完整伤害系统
**C++ 参考**: `Source/missiles.cpp:300-450`
**预计代码**: ~600行

#### 实现内容

1. **GetMissileVel - 速度计算** (~150行)
   ```rust
   pub fn get_missile_vel(
       missile_type: MissileType,
       source_pos: Point,
       target_pos: Point,
       speed: i32,
   ) -> (Point, i32) // (velocity, range)
   ```

   - 根据导弹类型计算速度
   - 计算方向向量
   - 计算飞行时间和射程

2. **MonsterMHit - 怪物伤害** (~200行)
   ```rust
   pub fn monster_m_hit(
       monster_id: usize,
       missile: &Missile,
       damage_type: DamageType,
       min_damage: i32,
       max_damage: i32,
       is_shifted: bool,
   ) -> i32 // 返回实际伤害
   ```

   - 计算基础伤害
   - 应用怪物抗性
   - 处理特殊怪物属性（免疫、反射等）
   - 触发怪物反应（AI状态变更）

3. **Plr2PlrMHit - 玩家伤害** (~150行)
   ```rust
   pub fn plr2plr_m_hit(
       player_id: usize,
       missile: &Missile,
       damage_type: DamageType,
       min_damage: i32,
       max_damage: i32,
       is_shifted: bool,
   ) -> i32
   ```

   - PvP 伤害计算
   - 应用护甲和抗性
   - 处理致命一击

4. **ObjectMHit - 对象伤害** (~50行)
   ```rust
   pub fn object_m_hit(
       object_id: usize,
       missile: &Missile,
   ) -> bool // 是否破坏对象
   ```

5. **测试** (~50行)
   - 速度计算测试
   - 伤害计算测试
   - 抗性测试

**成功标准**:
- ✅ 完整伤害计算系统
- ✅ 15个测试全部通过
- ✅ 与monster/player系统集成

---

### Day 3: 特殊导弹效果

**目标**: 实现爆炸、连锁闪电等特殊效果
**C++ 参考**: `Source/missiles.cpp:1500-3500` (各种 MI_* 函数)
**预计代码**: ~700行

#### 实现内容

1. **爆炸效果 (Explosion/Fireball)** (~200行)
   ```rust
   pub fn process_explosion(missile: &mut Missile) {
       // 范围伤害
       // 创建爆炸动画
       // 应用AOE效果
   }
   ```

   实现导弹类型：
   - Fireball
   - Meteor
   - Immolation
   - Apocalypse

2. **连锁闪电 (Chain Lightning)** (~150行)
   ```rust
   pub fn process_chain_lightning(missile: &mut Missile) {
       // 查找最近目标
       // 创建连锁导弹
       // 递减伤害
   }
   ```

3. **持续效果 (Elemental/Flame Wave)** (~150行)
   ```rust
   pub fn process_elemental_wave(missile: &mut Missile) {
       // 移动波纹
       // 范围检测
       // 持续伤害
   }
   ```

4. **召唤导弹 (Guardian)** (~100行)
   ```rust
   pub fn process_guardian(missile: &mut Missile) {
       // 生成守护者
       // 自动攻击
   }
   ```

5. **特殊导弹处理器映射** (~50行)
   ```rust
   pub fn get_missile_processor(
       missile_type: MissileType
   ) -> fn(&mut Missile)
   ```

6. **测试** (~50行)
   - 每种特殊效果测试
   - AOE范围测试
   - 连锁效果测试

**成功标准**:
- ✅ 10+种特殊导弹效果
- ✅ 12个测试全部通过
- ✅ 特效系统完整

---

### Day 4: 测试、集成与修复

**目标**: 综合测试和Bug修复
**预计代码**: ~200行

#### 实现内容

1. **导弹更新循环完善** (~100行)
   ```rust
   impl MissileManager {
       pub fn process_missiles_full(&mut self, game_state: &mut GameState) {
           // 遍历所有活跃导弹
           // 调用各自的processor
           // 检测碰撞
           // 应用效果
           // 清理已过期导弹
       }
   }
   ```

2. **集成测试** (~50行)
   - 端到端导弹测试
   - 多导弹交互测试
   - 性能测试

3. **Bug修复**
   - 修复碰撞检测问题
   - 修复伤害计算问题
   - 修复内存泄漏

4. **代码优化**
   - 性能优化
   - 代码重构
   - 文档完善

**成功标准**:
- ✅ 所有测试通过 (预计50+测试)
- ✅ 零编译警告
- ✅ 与游戏主循环集成

---

## 📈 预期成果

### 代码统计
```
missiles.rs: 4,206 → ~6,206 行 (+2,000)
新增测试: ~37 个
总测试: 853 → 890+
```

### C++ 对齐度
```
CheckMissileCol: 100%
MonsterMHit: 100%
Plr2PlrMHit: 100%
特殊导弹: 85% (核心10种100%, 其他待M67/M68)
```

### 功能完整度
- ✅ 完整碰撞检测系统
- ✅ 完整伤害计算系统
- ✅ 10+种特殊导弹效果
- ✅ 与combat/monster/player集成
- ✅ 50+测试验证

---

## 🔗 依赖关系

### 前置依赖
- ✅ M72-M75: Item Affixes (已完成)
- ✅ 基础combat系统
- ✅ monster_exact.rs
- ✅ player_exact.rs

### 后续里程碑
- M67: Monster AI (需要导弹系统支持怪物技能)
- M68: Player Combat (需要导弹系统支持玩家法术)

---

## 📝 注意事项

1. **严格遵循C++源码**
   - 每个函数必须与C++对齐
   - 保持相同的参数和返回值
   - 保持相同的逻辑流程

2. **完整测试覆盖**
   - 每个公开函数至少1个测试
   - 复杂函数需要多个测试覆盖边界情况
   - 所有测试必须通过才能标记完成

3. **集成验证**
   - 与combat系统集成测试
   - 与monster系统集成测试
   - 与player系统集成测试

4. **文档要求**
   - 每个函数都有C++源码引用
   - 复杂逻辑有详细注释
   - 完成后编写移植文档

---

**准备开始**: 确认理解需求后开始Day 1实现 ✅
