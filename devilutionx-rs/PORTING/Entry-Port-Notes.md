# Entry Port Memo (C++ DiabloMain ➜ Rust)

- Reference: `Source/diablo.cpp` (`RunGameLoop`, `DiabloMain` flow around lines 857 and 2696). Sequence mirrored in `src/main.rs`.
- Rust flow now: parse flags (`--spawn`, `--demo`, `--no-splash`/`-n`, `--data-dir`/`--save-dir`/`--config-dir`, `--lang`, `--record`, `--timedemo`, `--hellfire`/`--diablo`/`--vanilla` stubs), init keymap/padmap, apply path overrides, load core MPQs → MPQ version check → load options → apply option overrides (lang/shareware/hellfire/diablo) → create window → language MPQ → ApplicationInit → Lua stub → save options (skipped in demo) → detect archives → game/Hellfire archives (spawn fallback) → load text/player/spell/missile/monster/item/object/quest stubs → DiabloInit → save options (skipped in demo) → splash (uses MoviePlayer, skippable) → mainmenu → real `run_game_loop` with net/UI teardown stubs.
- Functional additions: reordered init to match C++, demo/no-splash gating for saves/splash, archive detection logs (diabdat/spawn/hellfire), MPQ version freshness check, path/config overrides via flags, data load stubs touch compiled spell/object tables, splash now routes through MoviePlayer, net_close/ui_reinitialize stubs after game loop.
- Still to port later: real movie playback/title dialog, full CLI behaviors (timedemo/record/create-reference/actions), real data-driven loaders (missile/monster/item/quest), proper net/provider/save/load logic, attract/credits dialogs, controller/menu parity, full cleanup parity.
- Verification: `cargo check` passes after latest changes (warnings unchanged).
