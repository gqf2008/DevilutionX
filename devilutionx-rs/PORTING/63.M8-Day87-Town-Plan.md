# M8 Day 87: Town Generation Implementation

**Date**: 2025-12-03
**Target**: Create town.rs module (~400 lines, 8 tests)
**M8 Progress**: Start → 10%

---

## 🎯 Objectives

实现 Tristram 城镇的基础地图加载系统，包括 4 个区域的拼接和特殊瓦片处理。

### C++ Reference
- `Source/levels/town.cpp` (390 lines)
- Functions: CreateTown(), FillSector(), FillTile(), InitTownPieces()

---

## 📋 Implementation Plan

### Phase 1: Module Setup (Day 87)

#### 1. **Create town.rs Module** (~50 lines)
```rust
//! Town (Tristram) level generation
//! C++ Source: Source/levels/town.cpp

use crate::levels::gendung::Dungeon;
use crate::levels::types::{DMAXX, DMAXY};

/// Town generator for Tristram (Level 0)
pub struct TownGenerator {
    // Town-specific state (if needed)
}

impl TownGenerator {
    pub fn new() -> Self {
        Self {}
    }

    /// Generate town level
    /// C++ equivalent: CreateTown (town.cpp:359)
    pub fn create_town(&mut self, dungeon: &mut Dungeon) {
        // Initialize base tiles
        self.init_base_tiles(dungeon);

        // Load 4 sectors
        self.fill_sector(dungeon, "sector1s.dun", 46, 46);
        self.fill_sector(dungeon, "sector2s.dun", 46, 0);
        self.fill_sector(dungeon, "sector3s.dun", 0, 46);
        self.fill_sector(dungeon, "sector4s.dun", 0, 0);

        // Apply special tiles
        self.init_town_pieces(dungeon);
    }
}
```

#### 2. **init_base_tiles()** (~15 lines)
```rust
/// Initialize all tiles to default value (426)
/// C++ equivalent: DrlgTPass3 initialization (town.cpp:194-200)
fn init_base_tiles(&self, dungeon: &mut Dungeon) {
    for y in 0..DMAXY {
        for x in 0..DMAXX {
            dungeon.tiles[x][y] = 213; // Default town tile (426/2)
        }
    }
}
```

#### 3. **fill_sector()** Stub (~30 lines)
```rust
/// Load sector .dun file data
/// C++ equivalent: FillSector (town.cpp:19-51)
///
/// # Arguments
/// * `filename` - Sector file (e.g., "sector1s.dun")
/// * `xi` - Upper-left X destination
/// * `yy` - Upper-left Y destination
fn fill_sector(&mut self, dungeon: &mut Dungeon, filename: &str, xi: usize, yy: usize) {
    // TODO: Day 88 - Load .dun file and parse
    // For now, just mark the area
    log::info!("Loading sector: {} at ({}, {})", filename, xi, yy);

    // Stub: Fill with placeholder tile
    for y in yy..(yy + 23).min(DMAXY) {
        for x in xi..(xi + 23).min(DMAXX) {
            dungeon.tiles[x][y] = 100; // Placeholder
        }
    }
}
```

#### 4. **init_town_pieces()** Stub (~40 lines)
```rust
/// Initialize special town tiles
/// C++ equivalent: InitTownPieces (town.cpp:148-194)
fn init_town_pieces(&self, dungeon: &mut Dungeon) {
    // Special tile mappings (dPiece -> dSpecial)
    // C++ uses dPiece values: 359, 357, 128, etc.

    for y in 0..DMAXY {
        for x in 0..DMAXX {
            let tile = dungeon.tiles[x][y];

            // Map special tiles (stub for now)
            match tile {
                179 => dungeon.special_tiles[x][y] = 1,  // 359/2
                178 => dungeon.special_tiles[x][y] = 2,  // 357/2
                64  => dungeon.special_tiles[x][y] = 6,  // 128/2
                // ... more mappings
                _ => {}
            }
        }
    }
}
```

#### 5. **Update Dungeon struct** (~20 lines)

**Location**: `src/levels/gendung.rs`

```rust
pub struct Dungeon {
    pub tiles: [[u8; DMAXY]; DMAXX],
    pub special_tiles: [[u8; DMAXY]; DMAXX],  // NEW: dSpecial equivalent
    // ... existing fields
}

impl Dungeon {
    pub fn new() -> Self {
        Self {
            tiles: [[0; DMAXY]; DMAXX],
            special_tiles: [[0; DMAXY]; DMAXX],  // NEW
            // ... existing fields
        }
    }
}
```

#### 6. **Module Registration** (~10 lines)

**Location**: `src/levels/mod.rs`

```rust
pub mod town;  // NEW
```

---

### Phase 2: Testing (Day 87)

#### Test 1: test_town_generator_new
```rust
#[test]
fn test_town_generator_new() {
    let gen = TownGenerator::new();
    // Should create without error
}
```

#### Test 2: test_init_base_tiles
```rust
#[test]
fn test_init_base_tiles() {
    let gen = TownGenerator::new();
    let mut dungeon = Dungeon::new();

    gen.init_base_tiles(&mut dungeon);

    // All tiles should be 213
    for y in 0..DMAXY {
        for x in 0..DMAXX {
            assert_eq!(dungeon.tiles[x][y], 213);
        }
    }
}
```

#### Test 3: test_fill_sector
```rust
#[test]
fn test_fill_sector() {
    let mut gen = TownGenerator::new();
    let mut dungeon = Dungeon::new();

    gen.fill_sector(&mut dungeon, "sector1s.dun", 46, 46);

    // Should mark area (stub)
    assert!(dungeon.tiles[46][46] > 0);
}
```

#### Test 4: test_create_town
```rust
#[test]
fn test_create_town() {
    let mut gen = TownGenerator::new();
    let mut dungeon = Dungeon::new();

    gen.create_town(&mut dungeon);

    // Should execute without panic
}
```

#### Test 5-8: test_init_town_pieces_*
```rust
#[test]
fn test_init_town_pieces() {
    let gen = TownGenerator::new();
    let mut dungeon = Dungeon::new();

    // Setup special tiles
    dungeon.tiles[10][10] = 179; // Maps to special 1

    gen.init_town_pieces(&mut dungeon);

    assert_eq!(dungeon.special_tiles[10][10], 1);
}
```

---

## 📊 Estimated Metrics

### Code Distribution
```
town.rs module creation    : 50 lines
init_base_tiles()          : 15 lines
fill_sector() stub         : 30 lines
init_town_pieces() stub    : 40 lines
Dungeon struct update      : 20 lines
Module registration        : 10 lines
Tests                      : 120 lines
Documentation              : 60 lines
-----------------------------------
Total                      : 345 lines
Target                     : 400 lines
```

---

## 🔜 Phase 2-3 Plan (Day 88-89)

### Day 88: .dun File Loading
- Implement real fill_sector() with file I/O
- Parse sector .dun format
- MegaTile to micro-tile conversion
- +150 lines, +3 tests

### Day 89: Warp Gate Logic
- Cathedral/Caves/Hell entrance states
- Hive/Grave quest areas (Hellfire)
- Poisoned water quest tile
- +100 lines, +4 tests

---

## 📈 Progress Tracking

```
M7 Complete: 3,454 lines (L2+L3+L4) ✅
M8 Start: Day 87 Town Framework → 400 lines
M8 Day 88: .dun Loading → 550 lines
M8 Day 89: Quest Logic → 650 lines
M8 Target: ~700 lines (Town complete)
```

---

## 🎯 Success Criteria

**Day 87 (Framework)**:
- ✅ town.rs module created
- ✅ TownGenerator struct
- ✅ create_town() orchestration
- ✅ init_base_tiles() complete
- ✅ Stub implementations for sectors
- ✅ special_tiles field added to Dungeon
- ✅ 8 tests passing
- ✅ Compiles without errors

**C++ Alignment**: 60% (framework complete, content stubs)

---

**Plan Created**: 2025-12-03
**Estimated Time**: 2 hours (Day 87 only)
**Complexity**: Medium (new module, file I/O deferred)
